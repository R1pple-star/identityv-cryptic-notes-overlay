# 加页手记 · 摸金地图叠层工具 — 重构执行计划 v2

> 版本：v2（2026-09-08）
> 基础：在原《重启计划》之上，补入重构安全网、评估闭环细则与 AI 协作工作规则。
> 使用方式：本文档同时是「给 AI 的指令源」——每个阶段都可整节复制给 Claude Code 作为任务 prompt。

---

## 0. 本次重构唯一的目标

**让「热键 → 识别种子 → 地图叠层精准重合」这一条链路在游戏内真实可靠。**

不是重写一个更漂亮的代码库。代码整洁是手段，不是目的。任何不服务于这条链路的改动都推迟。

MVP 用户流程（重构完成的标准）：

```
玩家刚进入口（图标在视野）
  → Ctrl+Shift+F
  → 截面板 → 入口引索匹配
  → 置信（分<0.10 且 overlap≥0.4）→ 两段式对齐 → 投影重合 ✅
  → 不置信 → 提示用户手动选「方向+门」或 3 点标定 → 投影重合 ✅
  → 无论成败，本次截图+结果自动归档进评估集
```

---

## 1. 锁定决策（沿用原计划，不变）

1. **识别 = 入口引索匹配**。放弃整图对比。诚实边界：仅「刚进入口 + 图标在视野 + 侧门/二楼」可信；正门形状固定分不出种子，走手动。
2. **对齐 = 两段式 + 手动兜底**。第一段：引索匹配自带 (尺度, 位置) 初始变换；第二段：`find_overlay_transform` 以 `hint_s` 局部精修；兜底：3 点标定（`affine_from_points`）。地图恒北朝上，只要缩放+平移，无旋转。
3. **连续跟随 = 推迟**。旧版跟随状态机从未游戏内实测，重构期不做。一次性对齐实测可靠后再立项。
4. **测量闭环**：单一评估 harness + 「刚进入口」标注集，每个改动对着它跑。

---

## 2. 阶段 0：安全网（先做，不许跳过）

> 目标：让后续任何一步都可回滚、可验证。预计半天。

| # | 任务 | 完成定义 |
|---|---|---|
| 0.1 | `git init`，**原样提交全部现有代码（含 16 个废文件）**，打 tag `legacy` | `git log` 有初始 commit；`git tag` 可见 legacy |
| 0.2 | 写 `.gitignore`：`captures/`、`校准输出/`、`图标检测检查/`、`__pycache__/`、`experiments/`、`*.pyc` | 大文件/产物不进仓库 |
| 0.3 | **存黄金基线**：运行现有全部自检（`find_seed_submap.py`、`entrance_match.py`、`verify_follow.py`、`verify_entrance_e2e.py`），把完整终端输出存为 `baseline/legacy_output.txt` | 基线文件入库，后续每次重构动作后重跑并 diff |
| 0.4 | 建 `requirements.txt`（opencv-python / numpy / Pillow / PySide6 / mss / pywin32），注明 Python 版本；建 venv 验证旧程序能跑 | 干净 venv 里 `pip install -r requirements.txt` 后 `python app.py` 可启动 |
| 0.5 | 建 `config.toml`（骨架见 §6），把路径/面板坐标/迷雾色/阈值全部外置；代码暂不改，先把文件立起来 | config.toml 入库 |
| 0.6 | 建 `CLAUDE.md`（骨架见 §7），顶部写硬约束 | CLAUDE.md 入库 |

**铁律：阶段 0 完成前，不删任何文件、不动任何逻辑。**

---

## 3. 阶段 1：清理（删废文件）

> 每个删除动作一个 commit，commit message 写清删了什么、为什么。

**3.1 删一次性诊断脚本（12 个）**
`_diag.py` `_diag2.py` `_diag3.py` `_render_cmp.py` `_zoom_cmp.py` `_metric.py` `_metric2.py` `_verify_score.py` `_verify2.py` `_verify3.py` `_verify4.py` `_verify5.py`
（删前 `git grep` 确认无任何模块 import 它们。）

**3.2 删废弃原型（4 个）**
`orb_match.py` / `analyze_match.py` / `auto_match_experiment.py` / `find_seed_loc.py`

**3.3 删被取代的 identification 函数**（⚠️ 顺序敏感）
- 先把 `find_overlay_transform` 及附属（`_revealed_mask`/`_match_at_scale`/`_subpixel_min`/`SCALES`/`ALIGN_SCALES`/`PAD`）挪到 `core/alignment.py`（属阶段 2，但必须先于本步）；
- 再删 `matcher.py` 的 `find_seed`、`find_seed_color` 和 `find_seed_submap.py` 的 `find_seed_submap`。

**3.4 可选删**：`view.py`、`capture_screen.py`（逻辑并入 capture/calib）。

**完成定义**：`python app.py` 仍能启动；黄金基线重跑，输出与 `baseline/legacy_output.txt` diff 为空（仅允许路径行差异）。

---

## 4. 阶段 2：抽取共享模块（不删功能，只搬家）

> 原则：**纯移动，不改逻辑**。每次抽取后立刻重跑基线。一个模块一个 commit。

**4.1 抽 `core/vision.py`**（从 `matcher.py`）
`classify_region`、`FOG_BGR`+迷雾剔除、`content_bbox`、`load_bgr`、`_find_icon`、`FIXED_PANEL`（改从 config 读）、`detect_fog_panel`、`map_is_open`。

**4.2 抽 `core/alignment.py`**（从 `find_seed_submap.py`，先于 §3.3）
`find_overlay_transform` + 附属函数/常量；`map_to_overlay_rgba`、`auto_align_overlay`、`affine_from_points`、`transform_point`（从 `matcher.py` 摘入）。

**4.3 消除第三份拷贝**
`calib_annotate.py` 删掉自己复制的 `classify_region`/`_find_icon`，改 `from core.vision import ...`。

**4.4 保留不动的干净模块**
`map_library.py` / `overlay.py` / `capture.py` / `hotkey.py` / `build_entrance_index.py` / `manage_materials.py` 原样保留，仅改 import 路径。

**完成定义**：无函数行为变更；基线 diff 为空；`matcher.py` 瘦身后剩余逻辑归位或删除。

---

## 5. 阶段 3：识别链路改造

| # | 任务 | 完成定义 |
|---|---|---|
| 5.1 | 小改 `entrance_match.py` 的 `find_seed_by_entrance`：返回内部 (尺度, 入口图标屏幕位置) 及参考图坐标（从 `{seed}_index.json`），供对齐第一段直接构造相似变换 | 返回值含构造 M 所需的全部量；自检脚本打印验证 |
| 5.2 | 接两段式对齐：第一段用 5.1 的变换；第二段 `find_overlay_transform(hint_s=第一段尺度)` 局部精修；过 overlap≥0.4 闸 | 在确认样本上（17.13/17.11 侧门）端到端跑通 |
| 5.3 | 接手动兜底：入口图标检不出 → UI 提供「手动选方向+门」（旧版 100% 路径，**保留**）与「3 点标定」两条路 | 两条兜底路径都可达、可出投影 |
| 5.4 | 补种子 28 一楼侧门引索（降低阈值重跑 `build_entrance_index.py 28` 或手动标注） | `entrance_index/28_侧门.png` 存在且匹配可用 |

---

## 6. 阶段 4：评估闭环（与阶段 3 并行搭建）

**6.1 标注集 `eval/labels.csv`**（入库，版本管理）

```csv
file,seed,floor,entrance_type,is_entrance_view,notes
17.13.06.98.png,17.13,1,侧门,1,刚进入口
23.41.x.png,23.41,1,正门,0,非入口视图-控制组
```

- 目标 30~50 张起步；`is_entrance_view=1` 的「刚进入口」样本是主评估集，其余作控制组（防假阳）。
- **评估只报主集 top-1/top-3 + overlap 闸通过率 + 控制组误报率**。

**6.2 单一 harness `eval/eval_match.py`**
以旧 `eval_match.py` 为起点改造：路径从 config 读；输出固定三行指标；`scan_shots.py`/`scan_entrance.py`/`verify_align.py` 等有用逻辑并入或退役。

**6.3 失败样本自动回收（新机制，重点）**
运行时每次匹配后，把「截图 + top3 分数 + overlap + 是否过闸 + 用户是否手动纠正」追加写入 `captures/log.csv`；用户手动纠正的样本自动复制到 `eval/inbox/`，定期标注进 labels.csv。
**实测即标注**——这是旧版最缺的数据闭环，彻底解决「实测不可靠但没现场数据」的问题。

---

## 7. 阶段 5：重写 app.py

- 保留：UI 外壳、热键分发、素材管理入口。
- 重做：状态/线程。手搓 `threading.Thread`+`Signal` 改为 `QThread` worker + 命令队列；跟随推迟期间**不做复杂状态机**，一次热键=一个任务=一个结果。
- 每次匹配走 §6.3 的日志归档。

---

## 8. 阶段 6：游戏内实测验收

满足以下全部条件才算重构完成：

1. eval 主集 top-1 准确率 ≥ 旧版基线（且控制组 0 误报）；
2. 游戏内实测 10 局：入口匹配置信成功率、手动兜底成功率、投影重合观感（肉眼验收，参考 `preview_*.png` 方式）有记录；
3. 失败样本全部自动进了 `eval/inbox/`；
4. `requirements.txt` + config 在新 venv 可复现运行。

验收过后，才允许立项「自动跟随」（届时从旧代码 `verify_follow.py` 的三级降级思路打捞，但必须游戏内实测驱动）。

---

## 9. 目标目录结构

```
jiaye-map/
  CLAUDE.md              # AI 硬约束（见 §11）
  config.toml            # 全部路径/常量/阈值（见 §10）
  requirements.txt
  baseline/legacy_output.txt
  app.py                 # 仅 UI 外壳 + 热键分发
  core/
    vision.py            # 视觉原语（分类/迷雾/图标/面板）
    alignment.py         # 对齐引擎（变换搜索/warp/标定）
    entrance.py          # 入口引索匹配（返回变换）
    map_library.py
  ui/
    overlay.py  hotkey.py  manage_materials.py
  eval/
    eval_match.py  labels.csv  inbox/
  assets/
    _icon_entrance.png
    entrance_index/
  experiments/           # 一次性脚本唯一合法去处（gitignore）
```

---

## 10. config.toml 骨架

```toml
[paths]
map_library   = "D:/Pictures/20260818摸金地图"   # 28种子×2楼层参考图
shot_library  = "D:/Videos/NVIDIA/IdentityV"     # 游戏截图测试样本
icon_template = "assets/_icon_entrance.png"
entrance_index= "assets/entrance_index"

[panel]
# 固定面板坐标，仅 1920x1080 有效（多分辨率 = TODO，现在不做）
resolution = [1920, 1080]
rect = [668, 166, 1064, 569]

[vision]
fog_bgr = [58, 47, 37]   # 迷雾色：冷色不是中性灰！暖度-21
fog_tol = 24

[match]
score_confident = 0.10   # 置信上限：分<0.10 且 overlap≥0.4
overlap_min = 0.40       # 二次闸，防小模板侥幸低分
scale_lo = 0.4
scale_hi = 1.0           # 种子匹配域；对齐域 ALIGN_SCALES 0.4-1.5
scale_step = 0.05        # 别改粗！粗档会擦肩
```

---

## 11. CLAUDE.md 骨架（新项目根目录必建）

```markdown
# 硬约束（违反=返工）
- 迷雾是冷色 BGR(58,47,37)，不是中性灰；revealed 判定必须按颜色距离额外剔除迷雾
- 匹配用 5分类 SQDIFF；禁止换 NCC（实测真种子进不了前6）
- 尺度搜索 0.05 步长，禁止改回粗档
- warpAffine 直传 src→dst 正向矩阵 M；禁止传逆矩阵
- 地图恒北朝上；禁止加旋转搜索
- 禁止用「合成面板」测试对齐；只用真实截图数据
- 置信 = 分<0.10 且 overlap≥0.4，两个闸缺一不可
- 一次性脚本只能进 experiments/；主目录禁止新建诊断脚本
- 每个改动后必须运行 python eval/eval_match.py 并贴出 top-1/top-3/误报率，
  指标劣于 baseline 则撤销改动
```

---

## 12. 与 AI（Claude Code + GLM）协作的工作规则

1. **一次任务只做一件事**，prompt 必须含：确切文件路径、函数名、验收命令、禁止碰的范围。例：「把 `classify_region` 和 `_find_icon` 从 matcher.py 移到 core/vision.py，只改 import 不改逻辑，完成后运行自检并贴输出；不许动其他文件。」
2. **验收用命令不用嘴**。不接受「应该没问题」，只看脚本输出数字。
3. **大改先出计划**：让模型先列「改哪些文件、动哪些函数、风险点」，人工确认后再动手。
4. **改崩了直接 `git checkout` 回滚**，缩小任务重来；不要让模型在原改动上「修」。
5. **像素级结论必须脚本测量**。模型读图只能判结构级对错，±像素偏移让它写代码量（旧版已踩坑）。
6. **每个阶段完成即 commit**；长任务后 `/clear` 重开会话，靠 CLAUDE.md + 本文档恢复上下文。
7. **本文档即任务源**：每节复制给 AI 就是合格 prompt，执行人在每阶段核对「完成定义」打勾。

---

## 13. 明确不做的事（防范围蔓延）

- ❌ 自动跟随 / 三级降级状态机（实测后再立项）
- ❌ 噩梦模式 3 楼层+地下室（架构预留即可）
- ❌ 多分辨率适配（config 留 TODO）
- ❌ UI 美化 / 打包发布
- ❌ 任何新匹配算法（NCC、SIFT、ORB、深度学习——都已验证或没必要）

---

## 14. 关键技术备忘（从旧项目说明继承，一字不改）

- **迷雾是冷色不是中性灰**：BGR(58,47,37) 暖度 -21，`classify_region` 会误判成通路(cls=3)。任何 `revealed = cls∈{1,2,3}` 的地方都要额外按颜色距离(`FOG_BGR` tol24)剔除迷雾。
- **尺度搜索要细**：0.05 步长(0.4-1.0)；真实面板适配尺度~0.7-0.85，粗档会擦肩→大模板错位、分虚高。
- **别换 NCC**：实测原始像素 NCC 真种子进不了前6，5分类 SQDIFF 更优。
- **overlap 闸是必备二次验证**：低分可能侥幸(小模板/小掩膜)。置信 = 分<0.10 且 overlap≥0.4。
- **地图恒北朝上**：匹配/对齐无需试旋转。
- **入口规则**：困难模式一楼 2 出入口(正门+侧门)、二楼 1 个，均有白箭头图标。正门固定最南边、房间形状固定（分不出种子）；侧门/二楼位置随机、房间形状各异（主要判别依据）。
- **warpAffine 语义**：默认传 src→dst 正向矩阵，内部自动求逆采样；传逆矩阵会投到屏幕外（踩坑，勿再试）。
- **模板=内容尺寸的"刀锋"**：`int()` 取整可能 tw>rw → 尺度被跳过；`PAD=12` 黑边放宽可行域（坐标按 `cx0-PAD+ml` 补偿）。
- **"合成面板"测不了匹配器**：无法复刻「游戏原生渲染 vs 手绘参考」噪声关系；用真实数据（同局两截图互为平移真值）。
- **参考图与游戏内非逐像素一致**：真种子匹配分上限~0.10-0.13，与错种子分差够大即可，渲染差异是正常的。
