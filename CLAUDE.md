# 加页手记 · 摸金地图叠层工具

项目根：`D:\claudecode out`。本文件是给 AI（Claude Code / GLM）的硬约束，违反即返工。
计划文档（按需指定段落让 AI 读，勿整读）：`重构计划-v2.md`、`重启计划.md`、`UI与流程改造计划.md`。

## 唯一目标
「热键 → 入口引索匹配 → 两段式对齐 → 投影重合」在游戏内真实可靠。不服务此链路的改动推迟。

## 硬约束（违反 = 返工）
- 迷雾是冷色 BGR(58,47,37) 非中性灰，`classify_region` 会误判成通路(cls=3)：一切 `revealed` 判定必须再按 `FOG_BGR`(tol24) 颜色距离剔除迷雾。
- 匹配只用 5 分类 SQDIFF，禁止 NCC（原始像素 NCC 真种子进不了前6）。
- 尺度步长 0.05，禁止粗档（真实面板尺度~0.7-0.85 落在粗档间隙）。
- `warpAffine` 直传 src→dst 正向矩阵，禁止逆矩阵（逆会把地图投到屏幕左上角外）。
- 地图恒北朝上，禁止旋转搜索（参考图亦北朝上）。
- 禁止用合成面板测试对齐，只用真实截图。
- 双闸并存缺一不可：入口置信闸 = 入口分<0.10 且 overlap≥0.40（种子ID可信）；对齐显示闸 = 重合分<0.30 且 overlap≥0.40（投影是否显示）。两闸目的不同。
- 一次性脚本只能进 `experiments/`，主目录禁建诊断脚本。
- 每个改动后跑 `python eval/eval_match.py` 并报告 top-1/top-3/误报率，劣于 baseline 则撤销。
- 改 `core/`（匹配/对齐）另跑 `python eval/verify_follow.py && python eval/verify_entrance_e2e.py`（基线 6/6）。

## 技术备忘（勿重蹈覆辙）
- 模板=内容尺寸的「刀锋」：`int()` 取整可让 tw>rw → 尺度被跳过；`PAD=12` 黑边放宽可行域（坐标按 `cx0-PAD+ml` 补偿）。
- 参考图与游戏内非逐像素一致：真种子分上限~0.10-0.13（非~0.02），分差够大即可。
- 入口：一楼 2 个(正门+侧门)、二楼 1 个，均带白箭头。正门固定最南+房形固定（分不出种子）；侧门/二楼位置随机、房形各异（主要判别依据）。
- 两段式对齐：M1 由「屏幕图标 ↔ 参考图图标({seed}_index.json)」+ 入口匹配尺度 s 构造（北朝上无旋转）；第二段 `find_overlay_transform(hint_s=s)` 精修，不过闸回退全搜。
- 像素级结论必须脚本测量，模型读图只判结构级对错。

## 工作规则
1. 一次任务一件事，prompt 含文件路径/函数名/验收命令/禁碰范围。
2. 验收只看脚本输出数字，不看「应该没问题」。
3. 大改先出计划，确认后动手。
4. 改崩了 `git checkout` 回滚缩小重来，不在原改动上修。
5. 阶段完成即 commit；长任务后 `/clear` 重开会话，靠本文件+计划文档恢复上下文。

## 不做（防范围蔓延）
- ❌ 自动跟随 / 状态机（游戏内实测可靠后再立项）
- ❌ 噩梦 3 楼层+地下室（架构预留即可）
- ❌ UI 美化 / 打包发布
- ❌ 新匹配算法（NCC/SIFT/ORB/深度学习，均已验证或没必要）
- ✅ 多分辨率适配（进行中）：`detect_fog_panel` 动态检测，`FIXED_PANEL` 退为 1920×1080 兜底；先在 1920×1080 验证不回归，再扩分辨率（需真实截图）。见 `UI与流程改造计划.md` 阶段C。

## 运行
```
cd "D:\claudecode out"
python app.py                       # 主程序（悬浮窗 UI + 热键分发）
python eval/eval_match.py           # 评估（top-1/top-3/误报率）
python eval/verify_follow.py        # 对齐原语回归（基线 6/6）
python eval/verify_entrance_e2e.py  # 入口匹配+对齐端到端回归
python build_entrance_index.py [种子号 ...]  # 重建入口引索
python calib_annotate.py <截图路径>  # 校准标注图
```
