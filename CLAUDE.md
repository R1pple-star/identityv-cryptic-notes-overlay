# 加页手记 · 摸金地图叠层工具

项目根：`D:\claudecode out`。本文件是给 AI（Claude Code / GLM）的硬约束，违反即返工。
重构总计划见本项目 `重构计划-v2.md` 与 `重启计划.md`。

## 唯一目标
让「热键 → 入口引索匹配 → 两段式对齐 → 投影重合」在游戏内真实可靠。代码整洁是手段不是目的。任何不服务这条链路的改动推迟。

## 硬约束（违反 = 返工）
- **迷雾是冷色 BGR(58,47,37)，不是中性灰**：暖度 -21，`classify_region` 会误判成通路(cls=3)。任何 `revealed = cls∈{1,2,3}` 的地方都要**额外按颜色距离(`FOG_BGR` tol24)剔除迷雾**，否则迷雾当通路污染模板。
- **匹配用 5 分类 SQDIFF；禁止换 NCC**（实测原始像素 NCC 真种子进不了前6）。
- **尺度搜索 0.05 步长，禁止改回粗档**（真实面板适配尺度~0.7-0.85 落在粗档间隙）。
- **warpAffine 直传 src→dst 正向矩阵 M；禁止传逆矩阵**（传逆会把地图投到屏幕左上角外，踩过坑）。
- **地图恒北朝上；禁止加旋转搜索**（用户确认；参考图亦北朝上）。
- **禁止用「合成面板」测试对齐**（无法复刻游戏原生渲染 vs 手绘参考的噪声关系）；只用真实截图数据。
- **双闸并存，缺一不可**：入口置信闸 = 入口分<`score_confident`(0.10) 且 overlap≥`overlap_min`(0.40)（种子ID是否可信）；对齐显示闸 = 重合分<`align_score_max`(0.30) 且 overlap≥0.40（投影是否该显示）。两闸目的不同。
- **一次性脚本只能进 `experiments/`；主目录禁止新建诊断脚本**。
- **每个改动后必须运行 `python eval/eval_match.py` 并贴出 top-1/top-3/误报率**；指标劣于 baseline 则撤销改动。
- **纯重构期（Phase 0-2）**：每步重跑 `python eval/verify_follow.py && python eval/verify_entrance_e2e.py`，与 `baseline/legacy_output.txt` 对应段 diff 必须为空（仅允许路径行差异）。

## 关键技术备忘（继承自旧项目说明，勿重蹈覆辙）
- 模板=内容尺寸的「刀锋」：`int()` 取整可能让 tw>rw → 尺度被跳过；`PAD=12` 黑边放宽可行域（坐标按 `cx0-PAD+ml` 补偿）。
- 参考图与游戏内非逐像素一致：真种子匹配分上限~0.10-0.13（非~0.02），分差够大即可，渲染略有差异是正常的。
- 入口规则：困难模式一楼 2 出入口(正门+侧门)、二楼 1 个，均有白箭头图标。正门固定最南边、房间形状固定（分不出种子）；侧门/二楼位置随机、房间形状各异（主要判别依据）。
- 两段式对齐：第一段由「屏幕入口图标 ↔ 参考图入口图标(cx,cy 来自 {seed}_index.json)」+ 入口匹配尺度 s 构造相似变换 M1（北朝上无旋转，`[[1/s,0,ix−cx/s],[0,1/s,iy−cy/s]]`）；第二段 `find_overlay_transform(hint_s=s)` 局部精修（s 即入口匹配尺度，因各裁图均原生分辨率、s 同义）；不过闸回退全搜。
- 像素级结论必须脚本测量，模型读图只能判结构级对错。

## 工作规则（与 AI 协作）
1. 一次任务只做一件事，prompt 含确切文件路径/函数名/验收命令/禁止碰的范围。
2. 验收用命令不用嘴，不接受「应该没问题」，只看脚本输出数字。
3. 大改先出计划（改哪些文件/动哪些函数/风险点），确认后再动手。
4. 改崩了直接 `git checkout` 回滚，缩小任务重来，不在原改动上「修」。
5. 每个阶段完成即 commit；长任务后 `/clear` 重开会话，靠本文件 + 计划文档恢复上下文。

## 明确不做的事（防范围蔓延）
- ❌ 自动跟随 / 三级降级状态机（Phase 6 游戏内实测可靠后再立项）
- ❌ 噩梦模式 3 楼层+地下室（架构预留即可）
- ✅ 多分辨率适配（**要做**，2026-09-10 改）：`detect_fog_panel` 动态检测迷雾面板，不依赖固定坐标；`FIXED_PANEL` 退为 1920×1080 兜底。见 `UI与流程改造计划.md` 阶段 C。动态化须先在 1920×1080 验证与旧基线一致不回归，再扩到其他分辨率（需真实截图样本）。
- ❌ UI 美化 / 打包发布
- ❌ 任何新匹配算法（NCC、SIFT、ORB、深度学习——都已验证或没必要）

## 运行
```
cd "D:\claudecode out"
python app.py              # 主程序（悬浮窗 UI + 热键分发）
python eval/eval_match.py  # 评估 harness（输出 top-1/top-3/误报率）
python eval/verify_follow.py        # 对齐原语回归自检（基线 6/6 通过）
python eval/verify_entrance_e2e.py  # 端到端入口匹配+对齐回归自检
python build_entrance_index.py [种子号 ...]  # 重建入口引索（如缺种子28一楼）
python calib_annotate.py <截图路径>  # 校准标注图
```
