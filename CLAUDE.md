# 加页手记 · 摸金地图叠层工具

项目根：`D:\claudecode out`。本文件是给 AI（Claude Code / GLM）的硬约束，违反即返工。
计划文档（按需指定段落让 AI 读，勿整读）：`UI与流程改造计划.md`（当前活跃，阶段C 多分辨率 / A4 右侧工具列未完）。

## 唯一目标
「热键 → 入口引索匹配 → 两段式对齐 → 投影重合」在游戏内真实可靠。不服务此链路的改动推迟。

## 硬约束（违反 = 返工）
- 迷雾是冷色 BGR(58,47,37) 非中性灰，`classify_region` 会误判成通路(cls=3)：一切 `revealed` 判定必须再按 `FOG_BGR`(tol24) 颜色距离剔除迷雾。
- 匹配只用 5 分类 SQDIFF，禁止 NCC（原始像素 NCC 真种子进不了前6）。**口径必须是 `[match] metric = "consistency"`（错配率）**：5 类嵌 R^4 正单纯形顶点（任意两类平方距离恒=2），4 通道 `TM_SQDIFF` /(2·权重和) ⇒ 分数 = 不一致像素占比 ∈[0,1]。**禁止回到旧「类号平方差」口径（`sqdiff`）**：它在类号 0..5 上做平方差，路(cls3)↔黑(cls0) 罚 9、房(cls2)↔路罚 1，实测真对齐下 4~6% 的离群像素吃掉 69~95% 的总分 ⇒ 正确对齐算成 0.47~2.56，永远过不了闸。详见「技术备忘」第 1 条。
- 尺度步长 0.05，禁止粗档（真实面板尺度~0.7-0.85 落在粗档间隙）。
- `warpAffine` 直传 src→dst 正向矩阵，禁止逆矩阵（逆会把地图投到屏幕左上角外）。
- 地图恒北朝上，禁止旋转搜索（参考图亦北朝上）。
- 禁止用合成面板测试对齐，只用真实截图。
- 双闸并存缺一不可：入口置信闸 = 入口分<`score_confident` 且 overlap≥0.40（种子ID可信）；对齐显示闸 = 重合分<`align_score_max` 且 overlap≥0.40（投影是否显示）。两闸目的不同。**阈值随口径走（config 注释已标）**：consistency 口径下 0.22 / 0.25；旧 sqdiff 口径对应 0.10 / 0.30。改口径必须同步改这两个数。
- 一次性脚本只能进 `experiments/`，主目录禁建诊断脚本。
- 每个改动后跑 `python eval/eval_match.py` 并报告 top-1/top-3/误报率，劣于 baseline 则撤销。
- 改 `core/`（匹配/对齐）另跑 `python eval/verify_follow.py && python eval/verify_entrance_e2e.py`。**实测基线（2026-09-16 核过；勿再引用「6/6」——e2e 脚本只有 4 个用例且无汇总计数）**：`verify_follow` 6/6；`verify_entrance_e2e` 主集 17.13✓ / 17.11✓（另 2 例是控制组，按设计判不出）。
- **当前总基线（2026-09-16，consistency 口径 + 09-15 实机 5 张真值入表）**：eval 主集 应答 top-1 **5/6**、top-3 6/6、overlap闸 5/6、拒答 9/15；控制组「确信=误报」3/11 —— 三例**全是 `best==GT`（实际误报 0）**。唯一失手是 213515（真25），真种子 0.2519 vs 错种子2 0.2386，**只差 5%** ⇒ 本来就该判"不确信"而非抛硬币。换口径前：4/6 · 4/6 · 2/6 · 控制组 1/11（实际误报 1）。
  （注：`7018c78` 曾记录 6/6，那是**权重按 w² 算错**的版本；改正为线性权重后 213515 翻掉。这条基线还会在第 2 步"墙重合度量"落地后重立。）

## 技术备忘（勿重蹈覆辙）
- **① 度量病理 = 2026-09 那次"不准"的总根因（最高优先级）**：旧口径在**类号**上做 `TM_SQDIFF`，惩罚 =（类号差)²，纯属编号巧合 —— 路(cls3)↔黑(cls0) 罚 **9**、房(cls2)↔路(cls3) 只罚 1，可两者都是"错"。
  - 实机后果（真种子+真尺度+锚定钉平移，ov 0.95）：类别一致率 **87%~92%**，分数却是 **0.473~2.560**，全部过不了 0.30 闸。213638 的 |差| 直方图 `{0:68697, 1:4580, 2:2366, 3:3489}` ⇒ **4.4% 的像素贡献 69% 的总分**；213605 是 6.1% 贡献 95%。
  - 由此派生「缩模板骗分」：模板越小离群像素越少、分越低 ⇒ **全搜恒钉尺度下界**（下界 0.30 时 5/5 取 0.300；把下界改成 0.25，5/5 立刻变 0.250~0.258 —— 跟着下界跑就是偏置的铁证）。这就是"分低"与"对齐对"脱钩的机制，幽灵相位与它同源。
  - 修法：`vision.simplex5()` / `to_simplex()` —— 5 类嵌 R^4 **正单纯形**顶点（任意两类平方距离恒 = 2），4 通道 `TM_SQDIFF` 响应 = 2×(不一致像素数) ⇒ 除以 2·权重和 = **错配率**。只用 4 通道，故 `matchTemplate` 调用次数**不变**（OpenCV 默认上限 4 通道，6 类不能直接 one-hot；5 类刚好）。
  - 实测收益：同一 M 分数 0.574 → **0.132**（与独立量出的 1−87% 自洽）；实机真种子排名 第7/第6名 → **第1名**；主集 top-1 4/6 → **6/6**；对齐层真对齐 0.131/0.137 过闸（旧口径下正确对齐永远过不了）。
  - ⚠️ **换口径不等于"分低就对了"**：错配率一样能被幽灵相位骗低。判对齐必须靠 `overlap` 闸（真种子 0.65~0.95 / 骗来的低分 ov 0.00~0.14）＋ 图标锚点交叉验证，别只看分。
  - ⚠️ **旧实现还有个隐蔽错误（2026-09-16 实测钉死）**：OpenCV `matchTemplateMask` 是**把 mask 乘在模板与图上再取平方差**，于是权重实际是 **w²**（`wall_boost=0.5` → 1.5 进、2.25 出；实测 2·Σw²·[不一致] 与 OpenCV 实值之比 = **1.0000**，Σw·[不一致] 只有 0.825）。所以：① 第一版单纯形实现（`7018c78`）算的不是"加权错配率"；② `wall_boost=0.5` 这个标定值是在平方口径下扫出来的，**换线性权重后需重标**（config 已标注）。现实现改为"数一致"的 CCORR，权重是线性的、与手工逐位一致。
- 模板=内容尺寸的「刀锋」：`int()` 取整可让 tw>rw → 尺度被跳过；`PAD=12` 黑边放宽可行域（坐标按 `cx0-PAD+ml` 补偿）。
- 游戏默认（不碰缩放）入口态 s≈0.40 在域外（2026-09-14 实测 6 局图标 k=1.0）：旧基线能匹配纯靠用户习惯性缩小地图（s≈0.9）偶然入域。救法=「图标当尺子·棘轮」：`_find_icon` 细网格（0.05 步）量出 k，`_crop_around_icon` 按 `max(1, k/icon_k_ref=0.44)` 放大裁样——**只放大不缩小**（缩小丢信息让小模板假获胜，17.13 实测 186px 裁样翻错种子）。s 与裁框大小无关，扩大取样梯子救不了缩放。
- 小模板假获胜：SCALES_ENT 下限扩到 0.35 后旧下限 0.6 的防小模板职责失效（17.13 实测种子16 以 112px 模板 0.039 反超真种子 0.046）——`MIN_TEMPLATE_PX=150`（真匹配模板世界覆盖 163-204 参考px）显式补上。裁样 half 上限 316（侧长 632×0.36=228 恰可搜；再大真尺度出域全'-'，710px 实测）。**已知副作用（2026-09-16 实测 17.11）**：裁样小的样本（208px，k≈0.45 的 8 月档）在真尺度 ~0.4 处 `min(tw,th)=83 < 150` 被整档跳过 ⇒ 真尺度**够不着**，只能在 s≥0.8 的错尺度上比。修它要先想清楚怎么同时保住"防小模板"——别只把 150 调小了事。
- 刚进门重雾样本（mask 8-15%）属信息缺失：快速失败闸拒答是正确行为，勿试图匹配（2026-09-14 晚 6 局入口图全此类）。eval_match 已镜像该闸：拒答单列、不计入 top-1/top-3 分母。探明后同场景可匹配（145440 实测 top1✓）。
- 单类退化：样本 mask 内最大单类占比>90%（大片均匀走廊/雾）时，分类 SQDIFF「样本覆盖处类别全等」⇒ 多种子精确同分 0.000（2026-09-14 实测 98% cls3）。救法=扩大取样梯子（0.25→0.32，只在仍退化时降档、首个非退化即停——0.32 档会把并列翻成错误种子假干净）。好样本单类 ≤0.78。纯走廊画面对齐无解（信息缺失，双向覆盖锚定/图标锚定实测均不过闸），3点标定兜底。
- 常驻屏幕检测干扰 NVIDIA 截图：跟随轮询每 tick 新建+销毁 mss 实例（GDI DC 开合 4次/秒）会让英伟达截图失效——`ui/capture.py` 必须常驻单例；投影隐藏期间不得截屏轮询。
- 参考图与游戏内非逐像素一致 —— 但**「真种子分上限 ~0.10-0.13」这句是旧口径的产物，已作废，勿再引用**：那是"类号平方差被少数离群像素打折"之后的数，量纲无意义（见技术备忘①）。新口径下真种子的分**就是这次对齐的错配率本身**：实测真种子 0.022~0.129（= 2%~13% 像素类别不一致）、错种子 0.25~0.45。分差和绝对量纲都可直接读。
- 墙色已标定（2026-09-15，用户提供纯色块标样）：通路墙 BGR(138,119,111) 冷-27 亮119、房间墙 BGR(114,122,139) 暖+25、两墙交界 (108,108,115)；走廊地 (104,86,78)、房间地 (79,88,102)，墙地色距≥99。代码留存：`vision.WALL_BGR/WALL_TOL=48`，`classify_region` 出 cls5，匹配两侧经 `walls_as_floors` 并回 2/3/1（=基线逐位一致）。墙类不进 SQDIFF 的真死因是**厚度惩罚**：游戏内墙 5-9px vs 参考墙 1-3px，直进重合分 0.046→1.31，参考侧膨胀±3px 也 1.3 级。（早先「两侧墙线画位不重合 6%/5%」的结论是**幽灵相位污染的误判**——用填充匹配的对齐当真值去测墙，错位 146px；图标锚定下墙重合 60-67%，素材与游戏内**同渲染器**。）
- **幽灵相位**（2026-09-15 用户圈图标破案）：迷宫走廊网格自相似，填充 SQDIFF 全搜可锁进错位 146px 的相位，分 0.046/overlap 0.80 双闸全过；锚点邻域给 4px 余量也会漂（墙重合 60%→4%）——景观平到任何余量都会被用掉。修法：`find_overlay_transform(hint_icon=(ref_cx,ref_cy,ix,iy))` 图标锚定**钉死平移只搜尺度**（`ANCHOR_RADIUS=0`），17.13 实测 score 0.046/ov 1.00/s 0.986/墙重合 60%。窗心在 ref 补边坐标系（原图坐标须减 cx0-PAD）。教训：好看的分+高 overlap 不等于对齐对——像素级验证须有独立锚点（图标）交叉。
- 跨侧亮度不可比：游戏内与参考图同位置亮度差 +20~40（通路 83 vs 61），且「亮冷≥68」类绝对阈值会把地面(~82)和墙(~119)一起抓成实心块（用户目测裁决）——任何按亮度切分必须逐图自适应且两侧同规，绝对阈值禁止。
- 墙距硬剔雾已证伪、软加权已上线（2026-09-15）：真走廊中位离墙 10-22px、雾 50-206px，但「离墙>12px 当雾剔」误杀 36-68% 真走廊（前沿雾侧墙不可见/宽走廊中心远/tol 漏暗墙段）——只能软加权。`wall_boost=0.5`（config [match]）：cls5 墙线±9px 带内模板权重 ×1.5，分差 +32~85%、e2e 01.11 案例确信误报（种子2@0.067过闸）被推破闸（0.112）；1.0 贴闸、2.0 炸闸（真种子/冒名一起抬，闸 0.10 是绝对值）。墙色雾免疫（雾最亮~75 < 墙带下沿~95）=「有墙色 ⇒ 已探明」严格成立。
- 入口：一楼 2 个(正门+侧门)、二楼 1 个，均带白箭头。正门固定最南+房形固定（分不出种子）；侧门/二楼位置随机、房形各异（主要判别依据）。
- 两段式对齐：M1 由「屏幕图标 ↔ 参考图图标({seed}_index.json)」+ 入口匹配尺度 s 构造（北朝上无旋转）；第二段 `find_overlay_transform(hint_s=s, hint_icon=…)` 图标锚定钉平移、全尺度扫 s 精修（防幽灵相位+入口 s 偏差——入口 s 可差真实 s ~0.09，17.13 实测 0.9 vs 0.986），不过闸回退全搜。
- 投影窗已设 `WDA_EXCLUDEFROMCAPTURE(0x11)`：mss/热键全屏截屏/OBS 均拍不到投影（e1 实测 品红占比1.000→0.000），防自截污染（墙体被判 structure → `map_is_open` 永判「开」死锁）。禁用 WDA_MONITOR(0x1)；换截屏后端需复验。设后不得再对该窗 `setWindowFlags`（重建 HWND 丢 affinity）。
- 图标锚定（2026-09-15 上线，`[match] anchor_pin`）：样本图标↔引索图标**重合后钉死平移**。依据 `experiments/e5_anchor_ab.py` 2×2 实测：**引索窗口放大到 2× 后全搜会炸**（主集 top-1 3/3→2/3、控制组低分确信 1→5——搜索域变大=自相似错位有机可乘），锚定把它抹平回 3/3 与 1，且控制组真种子排名 7~24 名→1~7 名。关 `anchor_pin` 即回退全搜（**注意：必须同时把引索窗口缩回 1×，否则是 E 档**）。
- **锚定 2026-09-16 三处修正（重要）**：
  1. **尺度窄带（`s0±anchor_band`）已废弃**，锚定只钉平移、尺度走全域 `SCALES_ENT`。理由：`s0=r_fine/k` 的残差是 ~0.09 量级（CLAUDE.md 旧记 0.9 vs 0.986），±0.06 窄带兜不住 —— 实测它把 `verify_entrance_e2e` 的 17.11（真种子18）挤成种子15。`config [match] anchor_band` 保留但代码不再读。
  2. **锚点出界 = 该种子弃权，绝不回退全搜**。旧写法是"本种子锚定一档都没落下 → 偷偷换全搜"，而全搜取每个尺度的全局最小、**恒 ≤ 锚点那一点的值** ⇒ "锚点用不了的种子"自动拿到更宽自由度，靠不公平优势压过真种子（17.11：种子15 锚点 0/13 档可用→回退全搜得 0.109，以 0.005 之差赢了种子18 的锚定值 0.114）。这与下面「锚定取分单调只升不降」是同一种病：**统计量不同就不可比**。修掉后主集 top-1 4/6→**6/6**。
  3. **代价（已知限制）**：锚定要求模板完整落在引索裁图内，侧门又在地图边界 ⇒ **11%~22% 的种子会因"锚点出界"被弃权**（实测 213638 弃 6/27、213605 弃 3/27、213515 弃 3/27）。当前净效果为正，但若将来出现"真种子恰好被弃权"的样本，优先考虑给锚定加**统一的**窗搜半径（所有种子同规，不许只放宽一部分），而不是恢复回退。
- `r_fine` 必须量准（2026-09-15）：`s = r_fine/k` 两式独立吻合（k=1.0→0.36 vs 实测 0.40；k=0.42→0.86 vs 旧基线 0.9）。**旧 `scale` 全部 82 个都是 0.4 = 粗网格 (0.4,0.5,…,3.0) 下界饱和，无信息**；细网格重测真值 0.30-0.42（中位 0.36）。重测已并入 `build_entrance_index.measure_fine_scale`（写 json 的 `scale_fine`），峰位偏移 ≤1px、NCC 0.78-0.92。`CROP_K` 同步 6→12。
- 贴边入口是锚定死角（2026-09-15 实测）：锚定要求模板**完整落在锚点周围**，需裁图每侧留 ≥ 模板世界覆盖(≈84~116px)。`CROP_K=12` 硬裁后贴边入口只剩 242px 宽（vs 456）→ 放不下 → 掉回全搜。试过「窗口整体平移塞进参考图」**反而更糟**（图标被推到裁图边缘，e2e 2/4→0/4、主集 3/3→2/3），已撤销并在 `crop_around` 注释留痕。
- 锚定取分**分数单调只升不降**（把「全局最小」换成「锚点那一点的值」）——入口置信闸是**绝对**阈值，真种子分被抬高时反而过不了闸。2026-09-16 把它归到「统计量不同不可比」这一大类里一并修掉（见锚定三条修正第 2 条）：**凡是"一部分候选按 A 规则打分、另一部分按更宽松的 B 规则打分"，排名就是假的。**
- **实机复现结论（2026-09-16，09-15 批 `captures/hotkey_20260915_*`，真值已入 `eval/labels.csv`）**——这是"离线渲染看着好、游戏内就是不行"的完整答案，四层叠加：
  1. **度量病理**（真因，见技术备忘①）：正确对齐被算成 0.47~2.56 永远不过闸。
  2. **全搜恒钉尺度下界**：低分是缩模板缩出来的，`overlap` 才是真判据。
  3. **锚定在实机大面积静默失效**：对齐层 5 张里 3 张「25 个尺度可用 **0** 档」（15 档"锚点出界"）、2 张仅 4 档 → `find_overlay_transform` 返回 None → `app.py` 静默回退**全搜**，幽灵相位防护等于没上线；而离线 17.13 那个案例锚点恰在界内，所以"渲染图验证通过"。入口层同类（11~22% 种子弃权）。
  4. **兜底显示骗人**：各闸全败 → `app.py::_show_centered_if_any` → `auto_align_overlay` 的 `min(pw/cw, ph/ch)` **把参考图缩放铺满面板**，比例与游戏内无关却画得工整 —— 用户报的「乱给一张素材、比例都不对」就是这张图，不是对齐结果。**待修**。
- **面板矩形右侧含游戏 UI（2026-09-16 实测）**：`FIXED_PANEL` 的右缘切进了游戏自带的导航列 —— "导航至出口" + 两个圆钮 + 竖滑条，约在面板 x≳940。这些像素被 `classify_region` 判成 cls1/2/3 混进 `_revealed_mask`，**成为参考图里永远不存在的假结构**（占探明掩膜 2%~25%；非地图画面那张 225827 的右 140px 结构占比 0.99）。列剖面可直接验证（掩膜在列 650~900 为 0、列 950~1050 又亮起来）。**待修**：面板右边界该收窄，或在掩膜里显式剔掉 UI 带。
- 热键路径**没有**"地图是否打开"的闸：`vision.map_is_open` 全仓**零调用**（只在 `ui/overlay.py` 一条注释里被提到）。在非地图画面（大厅/结算）按热键照样跑完匹配给出一个种子。**待修**。
- **eval 口径的坑（2026-09-16 加强版）**：主集 15 张里 **9 张被拒答**（`mask<20%` 快速失败闸），而 `top-1` 的分母**只算应答的那几张** ⇒ 头条数字显示 6/6，实际"按下热键后屏幕上出现的东西正确率"远低。评估真实体验必须把拒答和兜底显示都算作"没通过"。另外控制组的「确信=误报」会把**正确的**种子匹配也算成误报（本次 3/11 全部 `best==GT`，实际误报 0）——统计误报率时要按 `best==GT` 过滤，别直接读那个数。
- 评估口径的坑（2026-09-15）：控制组的「确信=误报」会把**正确的种子匹配**也算成误报。上线后控制组 0/11→1/11，那 1 例 `best==GT`、`overlap 1.00`、分 0.032，实际是正确识别。
- 拿老截图测匹配**不能用照片查看器**（2026-09-15 用户实测踩坑）：查看器会把图缩放到 85%/160% 并加标题栏位移，`panel.rect` 与真实尺度全对不上 → 真种子连前三都进不去，属预期而非算法退化。要测老截图走 `load_bgr` 直读文件（`eval/eval_match.py` 即此路径，或 `experiments/e5_render_match.py` 出图）。
- 像素级结论必须脚本测量，模型读图只判结构级对错。

## 工作规则
1. 一次任务一件事，prompt 含文件路径/函数名/验收命令/禁碰范围。
2. 验收只看脚本输出数字，不看「应该没问题」。
3. 大改先出计划，确认后动手。
4. 改崩了 `git checkout` 回滚缩小重来，不在原改动上修。
5. 阶段完成即 commit；长任务后 `/clear` 重开会话，靠本文件+计划文档恢复上下文。

## 不做（防范围蔓延）
- ✅ 自动跟随·第一步 done（e8f7f37，等游戏内复测）：投影显隐跟随游戏内 G 开关。判定=双特征 OR：雾色占比(FOG_BGR tol24)≥0.10 或 结构占比(cls2|cls3)≥0.10（重度探明开态 fog≈0 故必须 OR；单特征会被 Alt/F 画面变化误触发）；只在投影可见期间轮询；手动隐藏=暂停、点『显示地图』resume()；重开地图靠热键/显示按钮。设置 `auto_follow` 默认开。阈值复标：补拍关态截图（含 Alt/F 场景）进 `captures/follow_closed/` 后跑 `python experiments/e2_map_open_calib.py`。
- ❌ 其余跟随状态机（连续再对齐/自动重匹配），待开合跟随游戏内实测可靠后再立项。
- ❌ 噩梦 3 楼层+地下室（架构预留即可）
- ❌ UI 美化 / 打包发布
- ❌ 新匹配算法（NCC/SIFT/ORB/深度学习，均已验证或没必要）
- ✅ 多分辨率适配（代码 done，剩 C3 实测）：`panel_for_screen(w,h)`——1920×1080 精确=FIXED_PANEL、16:9 等比外推、config `[panel.rects]` 校准表、非16:9未校准拒绝。雾块/暗块动态检测已实测证伪，勿重试（测量在 `experiments/diag_panel_*.py`）。C3=真实非1920×1080 截图验证外推值+校准。
- ✅ eval 扩充（进行中）：标注集目标 30~50 张（**现 30 张** = 2026-09-15 之前 25 张 + 2026-09-15 实机批 5 张），`eval/inbox/` 失败样本定期标注进 `labels.csv`。
  **`eval/labels.csv` 是唯一的真值存档** —— 拿到新截图先把真种子/入口写进去（列：`file,seed,floor,entrance_type,is_entrance_view,notes`），不要每次重问；`is_entrance_view=1` 是主集（刚进入口）、按 `notes` 里写的 `0` 是控制组。
- **待办（2026-09-16 实机复现的新发现，按杠杆排序）**：
  1. **兜底显示骗人** → 不确信/拒答时**不投影**（或只显示样本）。不动任何匹配逻辑，直接消灭"乱给一张素材、比例不对"。
  2. **eval 换口径** → 按"按下热键后屏幕上最终出现的东西是否正确"统计（拒答与兜底都算不通过）。现口径把 9/15 拒答排除在分母外，永远看不见真实通过率。
  3. **热键路径加 `map_is_open` 闸** → 拒掉大厅/结算等非地图画面。
  4. **剔掉面板右侧游戏 UI 带**（面板 x≳940）→ 或收窄 `FIXED_PANEL`。
  5. **对齐层锚定的"出界"**：实机 5 张里 3 张「25 档可用 0」→ 静默回退全搜。修法同入口层（弃权 / 统一窗搜），但须先量清代价。
  6. **置信判据改「领先幅度」**：213515 真种子 0.268 vs 次名 0.291（只领先 8%），绝对阈值分不出可信与不可信。
- ✅ 重构收尾验收（进行中）：游戏内实测 10 局有记录（置信/兜底成功率、重合观感）后重构才算完成。

## 运行
```
cd "D:\claudecode out"
python app.py                       # 主程序（悬浮窗 UI + 热键分发）
python eval/eval_match.py           # 评估（top-1/top-3/误报率；主集 6/6 · 控制组实际误报 0）
python eval/verify_follow.py        # 对齐原语回归（基线 6/6）
python eval/verify_entrance_e2e.py  # 入口匹配+对齐端到端回归（主集 17.13✓ 17.11✓）
python build_entrance_index.py [种子号 ...]  # 重建入口引索
python calib_annotate.py <截图路径>  # 校准标注图
```

## 口径切换与回滚
- `config.toml [match] metric`：`"consistency"`（现网，错配率）↔ `"sqdiff"`（旧「类号平方差」）。
- **回滚必须成对改**：`metric` 和两个闸（`score_confident` / `align_score_max`）一起换 ——
  consistency 配 `0.22 / 0.25`，sqdiff 配 `0.10 / 0.30`。只改一个会让闸失去意义。
- 回滚已实测**零扰动**（2026-09-16）：切回 `sqdiff` + 旧闸，eval 数字与旧基线逐位一致
  （主集 4/6 · 4/6 · 2/6，控制组 1/11）。

## 诊断脚本（在 `experiments/`，出图在 `experiments/out/`）
> ⚠️ `experiments/` 在 `.gitignore` 里（一次性脚本，不入库）—— **这些脚本只存在于本机工作副本**。
> 换机器/清目录后需重写；下面记的是"要诊断这类问题时该造什么工具"。
| 脚本 | 用途 |
|---|---|
| `e9_live_compare.py` | 实机截图四行对比图（游戏 / **app 实际显示的铺满版** / 两段式对齐 / 样本↔引索） |
| `e10_anchor_audit.py` | 锚定分支审计：逐尺度为何被跳过、锚定 vs 全搜 vs 兜底 |
| `e11_gt_audit.py` | 真值审计：真种子 vs top1 种子的对齐分数对比 + 叠图 |
| `e12_cls_confusion.py` | 真对齐下的类别混淆矩阵（一致率 / 谁在贡献分数） |
| `e13_metric_anatomy.py` | 度量解剖：diff² 均值 vs min(diff²,1) vs 不一致率 |
| `e14_newmetric_trial.py` | 换口径试算·定种子层（真种子排名 前/后） |
| `e15_align_newmetric.py` | 换口径试算·对齐层（新口径选中 vs 现网选中 + 叠图） |
| `e16_1711_anchor.py` | 单样本锚定诊断（逐尺度锚定分/全搜分、可用档数） |
