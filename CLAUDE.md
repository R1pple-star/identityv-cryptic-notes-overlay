# 加页手记 · 摸金地图叠层工具

项目根：`D:\claudecode out`。本文件是给 AI（Claude Code / GLM）的硬约束，违反即返工。
计划文档（按需指定段落让 AI 读，勿整读）：`UI与流程改造计划.md`（当前活跃，阶段C 多分辨率 / A4 右侧工具列未完）。

## 唯一目标
「热键 → 入口引索匹配 → 两段式对齐 → 投影重合」在游戏内真实可靠。不服务此链路的改动推迟。

## 硬约束（违反 = 返工）
- 迷雾是冷色 BGR(58,47,37) 非中性灰，`classify_region` 会误判成通路(cls=3)：一切 `revealed` 判定必须再按 `FOG_BGR`(tol24) 颜色距离剔除迷雾。
- 匹配只用 5 分类 SQDIFF，禁止 NCC（原始像素 NCC 真种子进不了前6）。
- 尺度步长 0.05，禁止粗档（真实面板尺度~0.7-0.85 落在粗档间隙）。
- `warpAffine` 直传 src→dst 正向矩阵，禁止逆矩阵（逆会把地图投到屏幕左上角外）。
- 地图恒北朝上，禁止旋转搜索（参考图亦北朝上）。
- 禁止用合成面板测试对齐，只用真实截图。
- 双闸并存缺一不可：入口置信闸 = 入口分<0.10 且 overlap≥0.40（种子ID可信）；对齐显示闸 = 重合分<0.30 且 overlap≥0.40（投影是否显示）。两闸目的不同。
- 一次性脚本只能进 `experiments/`，主目录禁建诊断脚本。
- 每个改动后跑 `python eval/eval_match.py` 并报告 top-1/top-3/误报率，劣于 baseline 则撤销。
- 改 `core/`（匹配/对齐）另跑 `python eval/verify_follow.py && python eval/verify_entrance_e2e.py`（基线 6/6）。

## 技术备忘（勿重蹈覆辙）
- 模板=内容尺寸的「刀锋」：`int()` 取整可让 tw>rw → 尺度被跳过；`PAD=12` 黑边放宽可行域（坐标按 `cx0-PAD+ml` 补偿）。
- 游戏默认（不碰缩放）入口态 s≈0.40 在域外（2026-09-14 实测 6 局图标 k=1.0）：旧基线能匹配纯靠用户习惯性缩小地图（s≈0.9）偶然入域。救法=「图标当尺子·棘轮」：`_find_icon` 细网格（0.05 步）量出 k，`_crop_around_icon` 按 `max(1, k/icon_k_ref=0.44)` 放大裁样——**只放大不缩小**（缩小丢信息让小模板假获胜，17.13 实测 186px 裁样翻错种子）。s 与裁框大小无关，扩大取样梯子救不了缩放。
- 小模板假获胜：SCALES_ENT 下限扩到 0.35 后旧下限 0.6 的防小模板职责失效（17.13 实测种子16 以 112px 模板 0.039 反超真种子 0.046）——`MIN_TEMPLATE_PX=150`（真匹配模板世界覆盖 163-204 参考px）显式补上。裁样 half 上限 316（侧长 632×0.36=228 恰可搜；再大真尺度出域全'-'，710px 实测）。
- 刚进门重雾样本（mask 8-15%）属信息缺失：快速失败闸拒答是正确行为，勿试图匹配（2026-09-14 晚 6 局入口图全此类）。eval_match 已镜像该闸：拒答单列、不计入 top-1/top-3 分母。探明后同场景可匹配（145440 实测 top1✓）。
- 单类退化：样本 mask 内最大单类占比>90%（大片均匀走廊/雾）时，分类 SQDIFF「样本覆盖处类别全等」⇒ 多种子精确同分 0.000（2026-09-14 实测 98% cls3）。救法=扩大取样梯子（0.25→0.32，只在仍退化时降档、首个非退化即停——0.32 档会把并列翻成错误种子假干净）。好样本单类 ≤0.78。纯走廊画面对齐无解（信息缺失，双向覆盖锚定/图标锚定实测均不过闸），3点标定兜底。
- 常驻屏幕检测干扰 NVIDIA 截图：跟随轮询每 tick 新建+销毁 mss 实例（GDI DC 开合 4次/秒）会让英伟达截图失效——`ui/capture.py` 必须常驻单例；投影隐藏期间不得截屏轮询。
- 参考图与游戏内非逐像素一致：真种子分上限~0.10-0.13（非~0.02），分差够大即可。
- 墙色已标定（2026-09-15，用户提供纯色块标样）：通路墙 BGR(138,119,111) 冷-27 亮119、房间墙 BGR(114,122,139) 暖+25、两墙交界 (108,108,115)；走廊地 (104,86,78)、房间地 (79,88,102)，墙地色距≥99。代码留存：`vision.WALL_BGR/WALL_TOL=48`，`classify_region` 出 cls5，匹配两侧经 `walls_as_floors` 并回 2/3/1（=基线逐位一致）。墙类不进 SQDIFF 的真死因是**厚度惩罚**：游戏内墙 5-9px vs 参考墙 1-3px，直进重合分 0.046→1.31，参考侧膨胀±3px 也 1.3 级。（早先「两侧墙线画位不重合 6%/5%」的结论是**幽灵相位污染的误判**——用填充匹配的对齐当真值去测墙，错位 146px；图标锚定下墙重合 60-67%，素材与游戏内**同渲染器**。）
- **幽灵相位**（2026-09-15 用户圈图标破案）：迷宫走廊网格自相似，填充 SQDIFF 全搜可锁进错位 146px 的相位，分 0.046/overlap 0.80 双闸全过；锚点邻域给 4px 余量也会漂（墙重合 60%→4%）——景观平到任何余量都会被用掉。修法：`find_overlay_transform(hint_icon=(ref_cx,ref_cy,ix,iy))` 图标锚定**钉死平移只搜尺度**（`ANCHOR_RADIUS=0`），17.13 实测 score 0.046/ov 1.00/s 0.986/墙重合 60%。窗心在 ref 补边坐标系（原图坐标须减 cx0-PAD）。教训：好看的分+高 overlap 不等于对齐对——像素级验证须有独立锚点（图标）交叉。
- 跨侧亮度不可比：游戏内与参考图同位置亮度差 +20~40（通路 83 vs 61），且「亮冷≥68」类绝对阈值会把地面(~82)和墙(~119)一起抓成实心块（用户目测裁决）——任何按亮度切分必须逐图自适应且两侧同规，绝对阈值禁止。
- 墙距硬剔雾已证伪、软加权已上线（2026-09-15）：真走廊中位离墙 10-22px、雾 50-206px，但「离墙>12px 当雾剔」误杀 36-68% 真走廊（前沿雾侧墙不可见/宽走廊中心远/tol 漏暗墙段）——只能软加权。`wall_boost=0.5`（config [match]）：cls5 墙线±9px 带内模板权重 ×1.5，分差 +32~85%、e2e 01.11 案例确信误报（种子2@0.067过闸）被推破闸（0.112）；1.0 贴闸、2.0 炸闸（真种子/冒名一起抬，闸 0.10 是绝对值）。墙色雾免疫（雾最亮~75 < 墙带下沿~95）=「有墙色 ⇒ 已探明」严格成立。
- 入口：一楼 2 个(正门+侧门)、二楼 1 个，均带白箭头。正门固定最南+房形固定（分不出种子）；侧门/二楼位置随机、房形各异（主要判别依据）。
- 两段式对齐：M1 由「屏幕图标 ↔ 参考图图标({seed}_index.json)」+ 入口匹配尺度 s 构造（北朝上无旋转）；第二段 `find_overlay_transform(hint_s=s, hint_icon=…)` 图标锚定钉平移、全尺度扫 s 精修（防幽灵相位+入口 s 偏差——入口 s 可差真实 s ~0.09，17.13 实测 0.9 vs 0.986），不过闸回退全搜。
- 投影窗已设 `WDA_EXCLUDEFROMCAPTURE(0x11)`：mss/热键全屏截屏/OBS 均拍不到投影（e1 实测 品红占比1.000→0.000），防自截污染（墙体被判 structure → `map_is_open` 永判「开」死锁）。禁用 WDA_MONITOR(0x1)；换截屏后端需复验。设后不得再对该窗 `setWindowFlags`（重建 HWND 丢 affinity）。
- 像素级结论必须脚本测量，模型读图只判结构级对错。

## 工作规则
1. 一次任务一件事，prompt 含文件路径/函数名/验收命令/禁碰范围。
2. 验收只看脚本输出数字，不看「应该没问题」。
3. 大改先出计划，确认后动手。
4. 改崩了 `git checkout` 回滚缩小重来，不在原改动上修。
5. 阶段完成即 commit；长任务后 `/clear` 重开会话，靠本文件+计划文档恢复上下文。

## 不做（防范围蔓延）
- ✅ 自动跟随·第一步 done（e8f7f37，等游戏内复测）：投影显隐跟随游戏内 G 开关。判定=双特征 OR：雾色占比(FOG_BGR tol24)≥0.10 或 结构占比(cls2|cls3)≥0.10（重度探明开态 fog≈0 故必须 OR；单特征会被 Alt/F 画面变化误触发）；只在投影可见期间轮询；手动隐藏=暂停、点『显示地图』resume()；重开地图靠热键/显示按钮。设置 `auto_follow` 默认开。阈值复标：补拍关态截图（含 Alt/F 场景）进 `captures/follow_closed/` 后跑 `python experiments/e2_map_open_calib.py`。
- ❌ 其余跟随状态机（连续再对齐/自动重匹配），待开合跟随游戏内实测可靠后再立项。
- ❌ 噩梦 3 楼层+地下室（架构预留即可）
- ❌ UI 美化 / 打包发布
- ❌ 新匹配算法（NCC/SIFT/ORB/深度学习，均已验证或没必要）
- ✅ 多分辨率适配（代码 done，剩 C3 实测）：`panel_for_screen(w,h)`——1920×1080 精确=FIXED_PANEL、16:9 等比外推、config `[panel.rects]` 校准表、非16:9未校准拒绝。雾块/暗块动态检测已实测证伪，勿重试（测量在 `experiments/diag_panel_*.py`）。C3=真实非1920×1080 截图验证外推值+校准。
- ✅ eval 扩充（进行中）：标注集目标 30~50 张（现 25，含 2026-09-14 缩放批 16 张），`eval/inbox/` 失败样本定期标注进 `labels.csv`。
- ✅ 重构收尾验收（进行中）：游戏内实测 10 局有记录（置信/兜底成功率、重合观感）后重构才算完成。

## 运行
```
cd "D:\claudecode out"
python app.py                       # 主程序（悬浮窗 UI + 热键分发）
python eval/eval_match.py           # 评估（top-1/top-3/误报率）
python eval/verify_follow.py        # 对齐原语回归（基线 6/6）
python eval/verify_entrance_e2e.py  # 入口匹配+对齐端到端回归
python build_entrance_index.py [种子号 ...]  # 重建入口引索
python calib_annotate.py <截图路径>  # 校准标注图
```
