# 加页手记 · 摸金地图叠层工具

项目根：`D:\claudecode out`。本文件是给 AI（Claude Code / GLM）的硬约束，违反即返工。
未完计划：`docs/UI与流程改造计划.md`（阶段C 多分辨率 C3 实测 / A4 右侧工具列未完）。
（`跟随收口与UI改造计划.md` T1-T4 已全部落地，文档已删，细节见 git 历史与待办 11。）

## 唯一目标
「热键 → 入口引索匹配 → 两段式对齐 → 投影重合」在游戏内真实可靠。不服务此链路的改动推迟。

## 硬约束（违反 = 返工）
- 迷雾是冷色 BGR(58,47,37) 非中性灰，`classify_region` 会误判成通路(cls=3)：一切 `revealed` 判定必须再按 `FOG_BGR`(tol24) 颜色距离剔除迷雾。
- **匹配口径各层只有一种**（2026-09-16 起，旧口径已删，回滚走 git）：
  - **入口层 = 墙重合度量**：`1 − 墙双向覆盖率`（只比几何不比色块，备忘②）。必须有锚点，无锚点 = 该种子弃权。
  - **对齐层 = 错配率**：不一致像素的加权占比 ∈[0,1]（5 类嵌 R^4 正单纯形 / 数一致 CCORR，见备忘①）。
  - 两层的分都「越小越好」，**量纲不同、各配各的闸**（入口 0.45 / 对齐 0.30），别互相套用。
  - 禁止 NCC 做**匹配打分**（原始像素 NCC 真种子进不了前6；导航列开合检测的 NCC 是「固定 UI 在不在」的二值检测，不冲突）。禁止回到「类号平方差」sqdiff（病理见备忘①）。
- **⚠️ `cv2.minMaxLoc` 返回序是 `(minVal, maxVal, minLoc, maxLoc)`，minLoc 是第 3 个**：要最小代价位置必须写 `mn, _, ml, _ =`（写错的后果见备忘③）。
- 尺度步长 0.05，禁止粗档（真实面板尺度~0.7-0.85 落在粗档间隙）。
- `warpAffine` 直传 src→dst 正向矩阵，禁止逆矩阵（逆会把地图投到屏幕左上角外）。
- 地图恒北朝上，禁止旋转搜索（参考图亦北朝上）。
- 禁止用合成面板测试对齐，只用真实截图（合成面板无法复刻「游戏渲染 vs 手绘参考」噪声关系，断言会误报）。
- 双闸并存缺一不可：入口置信闸 = 入口分<`score_confident`(0.45) 且 overlap≥`overlap_min`(0.40)（种子ID可信）；对齐显示闸 = 重合分<`align_score_max`(0.30) 且 overlap≥0.40（投影是否显示）。两闸目的不同。⚠️ 显示闸**挡不住错种子**（错种子分一样漂亮），它只判"几何放得下"。
- 一次性脚本只能进 `experiments/`（gitignore，仅本机），主目录禁建诊断脚本。
- 每个改动后跑 `python eval/eval_match.py` 并报告 top-1/top-3/误报率，劣于 baseline 则撤销。
- 改 `core/`（匹配/对齐）另跑 `python eval/verify_follow.py && python eval/verify_entrance_e2e.py`；改跟随另跑 `python eval/e48_track_tick_live.py`（★真代码回归）；改 UI/主窗跑 `python eval/smoke_app_offscreen.py`。
- **当前总基线（2026-09-18，探明闸加逃生门之后）**：
  - **端到端口径（`eval/e26_e2e_truth.py`）= 按下热键后屏幕上最终出现的东西**：主集 44 张 → **出图且对 21/44 = 48%**、出图但错 3、藏图 0、拒答 20。演进：12/27%（显示闸0.25）→ 14/32%（0.30）→ 21/48%（结构闸加领先幅度逃生门）。**错图恒为同一场景 3 张**（北-1沙发门 23 被判成 北-1门 10，真种子连次名都不是，墙重合 58%），两轮放宽都没让它变多。
  - 旧口径（分母只算应答的）：主集 top-1 **21/24**、overlap 闸 22/24、拒答 20/44；控制组「确信=误报」4/11（真误报仅 1 例：16.42 选种子20 真9；另 3 例 `best==GT`，统计时按 `best==GT` 过滤）。`verify_follow` 6/6；`verify_entrance_e2e` 17.13✓/17.11✓。
  - **瓶颈在入口层，不在对齐层**：29 张真值上入口 top-1 ≈ 14/29 = 48%（逃生门只改「要不要拒答」不改排序）。「真种子 5/5 排第一、领先 17~58%」是 6 张小样本；`7018c78` 的 6/6 是权重按 w² 算错的版本（见备忘①末条）。

## 技术备忘（勿重蹈覆辙；每条 = 教训 + 关键数字）
- **① 度量病理 = 2026-09 "不准"的总根因**：旧口径在**类号**上做 `TM_SQDIFF`，惩罚=(类号差)² 纯属编号巧合（路↔黑罚 9、房↔路只罚 1）——4.4%~6.1% 的离群像素吃掉 69~95% 总分，真对齐（类别一致率 87~92%）算成 0.473~2.560 永远过不了 0.30 闸。派生「缩模板骗分」：模板越小分越低 ⇒ 全搜恒钉尺度下界（下界 0.30 时 5/5 取 0.300；改 0.25 立刻变 0.250~0.258）——**分低与对齐对脱钩的机制**。
  修法 `vision.to_match3()`：3 通道 one-hot + **数"一致"**（`TM_CCORR` FFT 快路，掩膜折进模板），`cost = 权重和 − 加权一致数`。实测同一 M 分 0.574→0.132、真种子第7/6名→第1、主集 4/6→6/6。
  ⚠️ 错配率一样会被幽灵相位骗低 ⇒ 判对齐必须靠 `overlap` 闸（真种子 0.65~0.95 / 骗来的 0.00~0.14）+ 图标锚点交叉验证。⚠️ 旧 `matchTemplateMask` 权重实为 w²（实测 2·Σw² 之比=1.0000），该路线已随 class 口径整体删除（`wall_boost` 键已删）。
- **② 墙重合度量 = 入口层唯一口径**：两侧各取墙掩膜 `classify_region()==5` → 按入口尺度缩放、锚定对齐 → 双向覆盖率（±2/3/4/6px 膨胀吸收厚度差 游戏5-9px vs 参考1-3px）→ 分 = `1−覆盖率`。强在：雾免疫（雾~75 < 墙带下沿~95）、位置敏感（墙是 1-9px 细线，平移 40px 覆盖率 0.83→0.30）、均匀区零贡献、天然拒答（几乎没墙=未探明 ⇒ 弃权）。实现 `core/entrance.wall_overlap/_scan_seed_wall`。**必须锚定**（墙重合只在锚点处评一次）；无锚点即弃权不许回退全搜——统计量不同不可比。
- **③ 取分与取点必须同源**（`minMaxLoc` 写成 `mn,_,_,ml` 的实机 bug：分漂亮、投影十万八千里，5/6 张中招，overlap 全 0.00~0.09）。为什么回归没抓住：锚定档 `radius=0` 响应面 1×1 天然免疫，离线走锚定、实机锚点出界静默回退全搜才中招。教训：**"离线通过、实机不行"时先问实机走的是不是另一条分支**（备忘⑩同款）。
- **④ 手框样本路径曾 100% 死**：锚点要「图标在样本内的偏移」，手框路径没传 `sample_origin` ⇒ 锚点恒 None ⇒ 全体弃权恒返回空（与框多大无关）。修法 `find_seed_by_entrance(..., sample_origin=(框左上角屏幕坐标))`。
- **⑤ 地图开合判据（2026-09-17 起）**：开 ⇔ 侧栏导航列 NCC ≥ `NAV_NCC_MIN`(0.45) **或** 雾 ≥ `FOG_OPEN_MIN`(0.30)。**结构占比退出判定**（游戏世界实测能到 0.51 会误判）。阈值只在 `core/vision.py` 一处。窗口化时导航列位移 NCC 掉到 0.34（负样本带 0.27 / 正样本 0.543 ⇒ 闸 0.45；邻域搜索试过会收窄分离带，不搜）⇒ **NCC 高=判开可信，NCC 低必须靠雾兜底**。`map_open_from_roi` 第三返回值 `nav_ok` = 主判据自己说开（非兜底）；雾兜底低精度高召回，它说"开"不够销毁 `_last_rgba`。回归：`eval/e30_map_open_gate.py`（旧关新开必须 0 + 负样本断言；张数随语料增长）、`eval/e32_follow_reopen.py`（7/7）。热键路径同源闸在 `_entrance_pipeline_impl`。
- **⑥ 尺度 s 不能靠对齐分选**：对齐分对 s 单调递增（s=0.25→0.079、真 0.35→0.091、0.65→0.357）——「缩模板骗分」发生在尺度轴上，"全域挑分最小"恒偏低且被 `ALIGN_SCALES` 下界截住（真尺度 0.22 < 下界 0.30 ⇒ 最放大态偏 ~40px），**没有任何分数闸能拦住"尺度错"**。真值法：图标锚点 `err(s)=|M_s(cx,cy)−屏幕图标|`，真尺度处尖谷 1~4px。两条已标定尺度源：**(a) 缩放滑条圆点 y**（`vision.ZOOM_CURVE_C` 三阶，y∈[315,805]→s∈[0.22,0.83]，精度 ±0.01，全屏专用，窗口化失效）；**(b) 图标尺子 `s≈0.374/k`**（k 由 `_find_icon` 量，误差 ≤0.011，但 k 量化 0.05 ⇒ 自身不确定度 ≈ s·0.05/k，s 大时 ±0.09）。游戏缩放绕玩家图标为中心（尺度变 3.7× 图标只挪 40px）。
- **⑦ 跟随·第二步（投影跟地图平移/缩放）**：设计在 `ui/track.py` 模块头，接线 `app._follow_tick → _maybe_track → _track_tick`。**不能重跑全搜**（1.5~2s 冻死主线程），走局部三档：帧差闸（<1.2 灰度级 ⇒ 没动，1ms）→ 近处小窗（±`TRACK_NEAR_R`=12px，**必须 < 半格周期 25px** 否则有幽灵）→ 单尺度全平移。防幽灵靠**证据边际**：新分须 ≤ `TRACK_MARGIN`(0.60)×旧分（真位移改善 72~92%、格内幽灵只 8~15%）。**尺度必须按优先级取滑条值，绝不挑分**；**尺度变了不许走小窗**（错尺度上小窗也能低分假接受）。尺度源协议：滑条(1ms,±0.01) → 图标尺子(200ms) → 都没有 ⇒ **不做全平移、就地判丢**（错尺度全平移能锁到偏 840px 且分比真尺度还低）。连 `TRACK_LOST_MAX`(2) 次判丢 ⇒ 隐藏投影 + 清 `_last_rgba`；**G 关地图不判丢不清图**（`is_open and trusted` 才清——关闭动画帧导航列 NCC 能到 −0.01 而雾兜底照样说"开"）。耗时：帧差/滑条 1ms、小窗 ~113ms、全平移 ~97ms、尺子 ~200ms；单线程主线程拖动时每 250ms 有 ~110ms 占空。回归 `eval/e33_follow_track.py`（10/10）、`eval/e45_zoom_follow_replay.py`（3/3）。
- **⑧ 跟随心跳日志会骗人**：限流会采样掉尖峰（1.5s 限流 vs 1s 拖动 ⇒ 35 条心跳全 `帧差0.00` 看着像"画面没动"其实真缩放了 40%）⇒ 帧差记**本轮峰值**、关键行 `force=True` 绕限流。**别拿"日志全是 0.00"当"画面没动"的证据**。同因：日志走 `appendHtml`，`<` 会吃掉后半句 ⇒ 自由文本必须 `html.escape`（已修）；**别用"日志没写全"当"代码没走到"的证据**。
- **⑨ 回归的"复刻陷阱"（本项目最贵教训）**：`e33`/`e45` 是**复刻** `_track_tick` 逻辑的副本（自己 import 常量，名字齐全），而真代码 `app.py` 漏 import `TRACK_DEADBAND_S` ⇒ 每次 NameError 只进 stderr ⇒ **跟随·第二步曾整块从未运行过**，三轮排查全在冤枉帧差闸。修法：`e48_track_tick_live.py` 用**真代码**回归（用户截图驱动真 `_maybe_track`，断言零异常）+ `_maybe_track` 套 try/except 把异常写进应用日志。**改跟随必跑 e48**；冒烟的早退分支不算覆盖。
- **⑩ 「按此种子对齐」两病根（已修）**：① 用旧截图（只在从未截过时才截 ⇒ 分数/重叠逐位重复的日志指纹）⇒ 当场截屏；② 无锚点无尺度的全搜（缩模板骗分：全搜 s≈0.39/分0.038/重叠1.00 照样过闸，真值 0.83）⇒ 复用 `_two_stage_align`，hint_s 取滑条读数、hint_icon 取入口下拉图标。入口选错 ⇒ 过不了闸回退全搜 = 旧行为，不会更差。
- **⑪ 锚定机制三要点**：①锚定只钉平移、尺度走全域（`s0±band` 窄带已废弃——残差 ~0.09 兜不住，把 17.11 挤成种子15）；②锚点出界 = 弃权，绝不回退全搜（全搜恒 ≤ 锚点值 ⇒ 不公平优势压真种子，修掉后主集 4/6→6/6）；③代价：对齐层模板常是整个面板（实测 100%），侧门在地图边界 ⇒ 实机可用档 4~12/25、个别 0/25，靠"锚定不过闸→回退全搜"兜住。锚定依据 `e5_anchor_ab`：引索 2× 放大后全搜会炸，锚定抹平；置 false ⇒ 入口层全体弃权。
- **⑫ 幽灵相位**：迷宫走廊网格自相似（格周期 25~26 ref px），填充全搜可锁进错位 146px 的相位、分/overlap 双闸全过。修法：`find_overlay_transform(hint_icon=…)` 图标锚定钉死平移只搜尺度（`ANCHOR_RADIUS=0`）。教训：**好看的分+高 overlap ≠ 对齐对，像素级验证须有独立锚点交叉**。
- **⑬ 墙色已标定**（纯色块标样）：通路墙 BGR(138,119,111)、房间墙 BGR(114,122,139)、交界 (108,108,115)；墙地色距 ≥99 ⇒ `WALL_BGR`+`WALL_TOL=48` 覆盖交界带不吞地面。墙类不进对齐层 SQDIFF 的死因是厚度惩罚（直进 0.046→1.31），但**入口层正是吃 cls5 的**（墙重合度量）。
- **⑭ 常量速查**：`MIN_TEMPLATE_PX=150`（真匹配模板覆盖 163-204 参考 px；调小前先想怎么防小模板假获胜——208px 小裁样在真尺度 0.4 处整档被跳过只能比错尺度）；`PAD=12` 黑边放宽刀锋可行域；`icon_k_ref=0.44` 棘轮只放大不缩小（缩小丢信息让小模板假获胜）；取样梯子 0.25→0.32 只在单类退化（>90%）时降档、首个非退化即停（0.32 档会把并列翻成错种子）；好样本单类 ≤0.78，纯走廊无解（信息缺失，3点标定兜底）。
- **⑮ 截屏与测试环境**：mss 必须常驻单例（每 tick 新建+销毁 DC 会毁 NVIDIA 截屏）；**窗口化 capture 面板几何不可信**（底行≈220 有任务栏、游戏窗位移过 ⇒ 那批对齐结果无意义，全屏=底行<60 才作数）；**拿老截图不能用照片查看器**（缩放+标题栏位移，走 `load_bgr` 直读）；**全屏布局下导航列在面板之外**（面板右缘 1732、列 1736~1900）⇒ **不要收窄 `FIXED_PANEL`**（"面板切进 UI 列"是查看器/窗口化的错觉）；**像素级结论必须脚本测量，模型读图只判结构级对错**。
- **⑯ eval 口径的坑**：主集被拒答的不进 top-1 分母 ⇒ 头条数字偏乐观（旧口径 6/6 实为应答的 6/6，真实看 e26）；控制组「确信=误报」要把 `best==GT` 的过滤掉再读。
- **其余机制**：两段式 M1 = 图标锚点 + 入口尺度 s 构造（`build_entrance_transform` 只用 s 与图标坐标，不用 mloc）；投影窗已设 `WDA_EXCLUDEFROMCAPTURE(0x11)`（mss/OBS 拍不到，防自截污染；设后不得 `setWindowFlags`）；跨侧亮度不可比（+20~40），任何按亮度切分必须逐图自适应且两侧同规，绝对阈值禁止；入口一楼 2 个（正门固定最南分不出种子、侧门主要判别依据）+ 二楼 1 个，均带白箭头；刚进门重雾样本属信息缺失，快速失败拒答是正确行为；**样本框大小不是"匹配不上"的变量**（结构占比几乎不随框大小变，拒绝的原因是那片区域本身稀）。

## 工作规则
1. 一次任务一件事，prompt 含文件路径/函数名/验收命令/禁碰范围。
2. 验收只看脚本输出数字，不看「应该没问题」。
3. 大改先出计划，确认后动手。
4. 改崩了 `git checkout` 回滚缩小重来，不在原改动上修。
5. 阶段完成即 commit；长任务后 `/clear` 重开会话，靠本文件恢复上下文。

## 已完成（防重做；细节看 git log）
- ✅ 自动跟随·第一步：投影显隐跟随 G 开关（判据见备忘⑤；地图关后 1s 降频续轮询，G 开回直接放回上次烘焙的整屏 RGBA 不重匹配；手动隐藏才真停）。
- ✅ 自动跟随·第二步：投影跟平移/缩放（备忘⑦；**2026-09-19 才真正跑起来**——备忘⑨的 NameError）。
- ✅ 换种子 + 会话内黑名单（主窗 🔄 换种子 = 拉黑当前 + 试下一候选；候选耗尽置灰；按热键清空黑名单。黑名单按 `self._tried_seed` 记，不读 `self._seed`——后者从下拉反解，对不上会永不缩小候选表）。
- ✅ 换楼层（主窗 🔀 换楼层 一楼⇄二楼；同步 entrance_combo 到 `ENTRANCE_FLOOR["二楼"]` 保锚点；走既有 `_align_to` 零新增匹配逻辑）。
- ✅ T2 UI 改造：主窗 152px（5 按钮 + 参考图摘要 + 一行状态 + 透明度滑块）；日志进独立窗（关窗不销毁）；下拉进「参考图…」对话框（combo 本体信号未断，最易回归点）；状态行按像素截断 + tooltip + WARN/ERROR 染色，全部写入走 `_set_status`；指引文案 `FIX_PATH="⋯ 更多→手动纠错→"`。
- ✅ 兜底显示骗人已修：不确信/不过闸一张图都不显示（`app.py::_refuse`），`auto_align_overlay`/`_show_centered` 已删；失败时连上次成功投影也隐藏（要"保留不动"改一行）。
- ✅ 探明闸逃生门：结构 <0.20 时若入口 top1 领先次名 ≥`lead_min`(0.25) 仍放行（e34 扫档：+7 对 0 错；0.20 开始多错、0.15 崩）。`eval_match`/`e26` 已镜像该口径。
- ✅ 多分辨率适配代码 done（`panel_for_screen`：1920×1080 精确/16:9 外推/校准表/非16:9 拒绝），C3 实测未做；雾块/暗块动态检测已证伪勿重试。

## 待办（按杠杆排序）
1. **锚定还要不要**：入口层是瓶颈（48%），而锚定代价正是"11~22% 种子弃权"、对齐层 4~12/25 档可用。minMaxLoc 修好后全搜可能已可靠。先量：① 全搜对 09-15 那批 146px 幽灵案例重测；② 全搜 vs 锚定在标注集 top-1 对拍。若可靠 ⇒ 锚定机制可简化，待办 2 大半收益不必等它。
2. **入口闸 0.45 改看领先幅度**：180203 种子判对却卡 0.496 只算"不确信"；wall 口径下真种子领先分布还没量过。
3. **把墙重合用到对齐层（潜在最大收益）**：墙是尖的 ⇒ 幽灵相位可能根治、图标锚定可能不再必要，顺带解待办 1。先在 e2e/eval 量，别直接上。
4. **热键路径尺度窄带**：滑条已准到 ±0.01 全量程，折成 `s_ui±0.05` 传 `find_overlay_transform(scales=…)` ⇒ 25 档→5 档（1.5s→300ms）+ 消掉尺度平坦错误源 + 解掉最放大态 0.22<下界 0.30 的截断。**必须先对拍**（标注集比 窄带 vs 全域 的 top-1/拒答/出图且错）。
5. **C3 多分辨率实测**（`docs/UI与流程改造计划.md`）：真实非 1920×1080 截图验证外推值 + 校准表。
6. **重构收尾验收**：游戏内实测 10 局（置信/兜底成功率、重合观感）后重构才算完成。

## 运行
```
cd "D:\claudecode out"
python app.py                       # 主程序（悬浮窗 UI + 热键分发）
python eval/eval_match.py           # 旧口径（分母只算应答的，头条数字偏乐观）
python eval/e26_e2e_truth.py        # **真实口径**：按下热键后屏幕上出现的东西
python eval/verify_follow.py        # 对齐原语回归（基线 6/6）
python eval/verify_entrance_e2e.py  # 入口匹配+对齐端到端回归（17.13✓ 17.11✓）
python eval/e17_perf.py             # 热键主链路耗时（改匹配必跑）
python eval/e30_map_open_gate.py    # 地图开合判据对拍 + 负样本断言（改判据必跑）
python eval/e32_follow_reopen.py    # 跟随开合链路（7/7）
python eval/e33_follow_track.py     # 跟随·第二步（10/10，复刻版）
python eval/e45_zoom_follow_replay.py # 缩放跟随（3/3，复刻版）
python eval/e48_track_tick_live.py  # **★跟踪主体真代码回归**（改跟随必跑）
python eval/smoke_app_offscreen.py  # 离屏冒烟（主窗 UI/换种子/换楼层/投影窗/跟踪接线）
python build_entrance_index.py [种子号 ...]  # 重建入口引索
python build_ui_template.py <全屏实机截图 ...>  # 重建导航列模板 assets/ui_nav_column.png
python calib_annotate.py <截图路径>  # 校准标注图
```

## 口径
- 入口层 = 墙重合（`1 − 双向覆盖率`，闸 `score_confident = 0.45`）。
- 对齐层 = 错配率（闸 `align_score_max = 0.30`）。
- 两层都「越小越好」，但量纲不同，闸不能互换。旧口径已删，回滚走 git。
- 参考图与游戏内非逐像素一致，但新口径下真种子分 = 对齐错配率本身：实测真种子 0.022~0.129、错种子 0.25~0.45，分差可直接读。

## 真值与脚本存放
- **`eval/labels.csv` 是唯一真值存档**（现 59 行）：新截图先把 真种子/入口 写进去再干活，不要重复问用户。列：`file,seed,floor,entrance_type,is_entrance_view,notes`；`is_entrance_view=1` 是主集、notes 写 `0` 是控制组。`eval/inbox/` 收失败样本，定期标注进表。
- 关键回归已入库 `eval/`（见「运行」）；一次性诊断脚本在 `experiments/`（**gitignore，仅本机**，换机器需按需重写，下表是"该造什么工具"的索引）：

| 脚本（experiments/，本机） | 用途 |
|---|---|
| `e9_live_compare.py` | 实机截图四行对比图（游戏/app显示/两段式对齐/样本↔引索） |
| `e10_anchor_audit.py` | 锚定分支审计：逐尺度为何被跳过、锚定 vs 全搜 vs 兜底 |
| `e11_gt_audit.py` | 真值审计：真种子 vs top1 对齐分数 + 叠图 |
| `e12_cls_confusion.py` | 真对齐下的类别混淆矩阵 |
| `e13_metric_anatomy.py` | 度量解剖：diff² 均值 vs min(diff²,1) vs 不一致率 |
| `e14_newmetric_trial.py` / `e15_align_newmetric.py` | 换口径试算（定种子层/对齐层） |
| `e16_1711_anchor.py` | 单样本锚定诊断（逐尺度锚定分/全搜分/可用档数） |
| `e18_metric_equiv.py` | 两种实现逐种子对拍 |
| `e19_wall_overlap.py` | 墙重合可行性 + 位置敏感对照 |
| `e20_live_0916.py` | 实机批审计：样本画像/入口/hand框/锚定/全搜 vs 锚定 |
| `e21_anchor_geom.py` | 逐尺度锚点落点与越界方向 |
| `e22_ccorr_verify.py` | CCORR 响应 vs 手工逐位对拍（验证度量实现） |
| `e23_sample_size.py` | 样本大小 vs 结构占比（回答"框太小"） |
| `e27/e28/e29/e31` | 导航列 NCC 标定系列（开合判据） |
| `e34_evidence_gate.py` | 探明闸复核：扫「结构≥0.20 或 领先≥X」（得 0.25） |
| `e35/e36/e37/e38` | 尺度系列：滑条定位/对齐分vs尺度曲线/视频标定(已被e46取代)/三策略对拍 |
| `e47_zoom_motion_gate.py` | 帧差闸测量（拖缩放时帧差 ≥2.71，闸 1.2） |
| `e46_slider_recalib.py` | 滑条重标定（**已入库 eval/**，产生 ZOOM_CURVE_C） |
| `diag_*` / `measure_*` | 各类一次性诊断 |
