# 加页手记 · 摸金地图叠层工具

项目根：`D:\claudecode out`。本文件是给 AI（Claude Code / GLM）的硬约束，违反即返工。
计划文档（按需指定段落让 AI 读，勿整读）：**`跟随收口与UI改造计划.md`（当前活跃：跟随收口 T1 → UI 折叠 T2 → 换种子+黑名单 T3，含 2026-09-18 实机日志的两条确定 bug）**、`UI与流程改造计划.md`（阶段C 多分辨率 / A4 右侧工具列未完）。

## 唯一目标
「热键 → 入口引索匹配 → 两段式对齐 → 投影重合」在游戏内真实可靠。不服务此链路的改动推迟。

## 硬约束（违反 = 返工）
- 迷雾是冷色 BGR(58,47,37) 非中性灰，`classify_region` 会误判成通路(cls=3)：一切 `revealed` 判定必须再按 `FOG_BGR`(tol24) 颜色距离剔除迷雾。
- **匹配口径各层只有一种**（2026-09-16 起，旧的两套已删，回滚走 git）：
  - **入口层** = **墙重合度量**：`1 − 墙双向覆盖率`（只比几何不比色块，见技术备忘②）。**必须有锚点**，无锚点 = 该种子弃权。
  - **对齐层** = **错配率**：不一致像素的加权占比 ∈[0,1]（5 类嵌 R^4 正单纯形 / 数一致 CCORR，见技术备忘①）。
  - 两层的分都「越小越好」，**量纲不同、各配各的闸**（入口 0.45 / 对齐 0.30），别互相套用，也别照搬别层的分去调本层的闸。
  - 禁止 NCC（原始像素 NCC 真种子进不了前6）。禁止回到「类号平方差」sqdiff（病理见技术备忘①：真对齐算成 0.47~2.56 永远过不了闸）。
- **⚠️ `cv2.minMaxLoc` 的返回序是 `(minVal, maxVal, minLoc, maxLoc)`** —— minLoc 是**第 3 个**。凡是要「最小代价的位置」必须写 `mn, _, ml, _ =`。写错成 `_, _, _, ml` 的后果见技术备忘③（分数漂亮、投影十万八千里，实机 5/6 张中招）。
- 尺度步长 0.05，禁止粗档（真实面板尺度~0.7-0.85 落在粗档间隙）。
- `warpAffine` 直传 src→dst 正向矩阵，禁止逆矩阵（逆会把地图投到屏幕左上角外）。
- 地图恒北朝上，禁止旋转搜索（参考图亦北朝上）。
- 禁止用合成面板测试对齐，只用真实截图。
- 双闸并存缺一不可：入口置信闸 = 入口分<`score_confident`(0.45) 且 overlap≥`overlap_min`(0.40)（种子ID可信）；对齐显示闸 = 重合分<`align_score_max`(0.30) 且 overlap≥0.40（投影是否显示）。两闸目的不同。
- 一次性脚本只能进 `experiments/`，主目录禁建诊断脚本。
- 每个改动后跑 `python eval/eval_match.py` 并报告 top-1/top-3/误报率，劣于 baseline 则撤销。
- 改 `core/`（匹配/对齐）另跑 `python eval/verify_follow.py && python eval/verify_entrance_e2e.py`。
- **当前总基线（2026-09-18，探明闸加逃生门之后）**：
  - **端到端口径（待办 2，`experiments/e26_e2e_truth.py`）= "按下热键后屏幕上最终出现的东西"**：主集 44 张 → **出图且对 21/44 = 48%**、出图但错 3、藏图 0、**拒答 20**。**这才是真实通过率**。
    演进：12/27%（显示闸 0.25）→ 14/32%（显示闸→0.30，待办 7）→ **21/48%（结构闸加领先幅度逃生门，2026-09-18）**。
    **错图一直是同一个场景的 3 张**（北-1沙发门 23 被判成 北-1门 10），两轮放宽都没让它变多。
  - 旧口径（分母只算应答的）：主集 top-1 **21/24**、overlap 闸 22/24、拒答 20/44；控制组「确信=误报」4/11（1 例真误报：16.42 选种子20 真9；另 3 例 `best==GT`）。`verify_follow` 6/6；`verify_entrance_e2e` 17.13✓/17.11✓。
  - **入口层在这批 29 张真值上 top-1 ≈ 14/29 = 48%**（逃生门只改「要不要拒答」，不改入口层排序） —— 之前记的「真种子 5/5 排第一、领先 17~58%」是 **6 张样本**上的数，样本一多就掉下来。**瓶颈在入口层，不在对齐层。**
  - 已知失败场景：**北-1沙发门(23) 被稳定判成 北-1门(10)**（171442/171453/171503/171521 四张，真种子连次名都不是，墙重合 58%）。
  - 注意 `7018c78` 曾记录 6/6，那是**权重按 w² 算错**的版本（见技术备忘①末条）；改正为线性权重后 213515 翻掉。

## 技术备忘（勿重蹈覆辙）
- **① 度量病理 = 2026-09 那次"不准"的总根因（最高优先级）**：旧口径在**类号**上做 `TM_SQDIFF`，惩罚 =（类号差)²，纯属编号巧合 —— 路(cls3)↔黑(cls0) 罚 **9**、房(cls2)↔路(cls3) 只罚 1，可两者都是"错"。
  - 实机后果（真种子+真尺度+锚定钉平移，ov 0.95）：类别一致率 **87%~92%**，分数却是 **0.473~2.560**，全部过不了 0.30 闸。213638 的 |差| 直方图 `{0:68697, 1:4580, 2:2366, 3:3489}` ⇒ **4.4% 的像素贡献 69% 的总分**；213605 是 6.1% 贡献 95%。
  - 由此派生「缩模板骗分」：模板越小离群像素越少、分越低 ⇒ **全搜恒钉尺度下界**（下界 0.30 时 5/5 取 0.300；把下界改成 0.25，5/5 立刻变 0.250~0.258 —— 跟着下界跑就是偏置的铁证）。这就是"分低"与"对齐对"脱钩的机制，幽灵相位与它同源。
  - 修法：`vision.to_match3()` —— 5 类里只给 1/2/3 建 3 通道 one-hot，**数"一致"**（互相关）而不是数"不一致"：`cost = 掩膜权重和 − 加权一致像素数`，除以权重和 = **错配率**。掩膜**折进模板**（掩膜外填 0）⇒ 可走不带 mask 的 `TM_CCORR` **FFT 快路**（带 mask 会退化成最慢的逐点直算）。只用 3 通道是因为掩膜内模板类只可能是 1/2/3（0/4/5 都在掩膜外），参考侧落在那几类的像素三通道全 0、自动计为不一致 —— 逐位无损。
  - 实测收益：同一 M 分数 0.574 → **0.132**（与独立量出的 1−87% 自洽）；实机真种子排名 第7/第6名 → **第1名**；主集 top-1 4/6 → **6/6**；对齐层真对齐 0.131/0.137 过闸（旧口径下正确对齐永远过不了）。
  - ⚠️ **换口径不等于"分低就对了"**：错配率一样能被幽灵相位骗低。判对齐必须靠 `overlap` 闸（真种子 0.65~0.95 / 骗来的低分 ov 0.00~0.14）＋ 图标锚点交叉验证，别只看分。
  - ⚠️ **旧实现还有个隐蔽错误（2026-09-16 实测钉死）**：OpenCV `matchTemplateMask` 是**把 mask 乘在模板与图上再取平方差**，于是权重实际是 **w²**（`wall_boost=0.5` → 1.5 进、2.25 出；实测 2·Σw²·[不一致] 与 OpenCV 实值之比 = **1.0000**，Σw·[不一致] 只有 0.825）。那条贴墙增益路线已随 class 口径整体删除（`wall_boost` 键也删了）——它的思路正是"墙最可靠"，而墙重合度量就是这条思路走到头。
- **② 墙重合度量 = 入口层唯一口径（2026-09-16 用户提议落地）**：不信"5 类色块平均"，改信**几何**。
  - 步骤：两侧各取墙掩膜 `classify_region()==5` → 样本墙按入口尺度 s 缩放、锚定落点对齐 → **双向覆盖率**（自己有多少比例的墙落在「对方墙膨胀 r 像素」带上，两方向取平均）→ 分 = `1 − 覆盖率`（沿用"越小越好"，故下游排序/闸门/日志语义不变）。实现：`core/entrance.wall_overlap/_scan_seed_wall`。
  - 为什么强：① **雾免疫**（墙色雾免疫：雾最亮 ~75 < 墙带下沿 ~95）；② **位置敏感** —— 墙是 1-9px 细线，一挪就错开（实测平移 40px 覆盖率 0.83→0.30 / 0.78→0.36 / 0.56→0.18）；③ **均匀区零贡献** —— 大片走廊/房间本来就没墙，而那正是色块法最没判别力、且入口样本最常见的形态；④ 厚度差（游戏 5-9px vs 参考 1-3px）用 ±2/3/4/6px 膨胀容差吸收，不要求逐像素相等。
  - 实测（6 张有真值样本）：真种子 **5/5 排第一**，领先次名 **17%~58%**（色块法在 213515 上只领先 5%、翻错）。eval 主集 top-1 5/6 → **6/6**；入口匹配耗时 2005 → **756 ms**（比色块法**还快**，因为不用 matchTemplate）。
  - 天然拒答：样本几乎没墙（未探明）时全部尺度弃用 ⇒ 该种子弃权 ⇒ 全体弃权即"无结果"，正好落在"刚进门信息缺失应拒答"上。
  - **硬性要求**：必须锚定（墙重合只在锚点处评一次）。无锚点即**弃权**，不许回退全搜 —— 统计量不同不可比（同下面「锚定取分单调只升不降」那条）。
- **③ 取分与取点必须来自同一张响应图的同一位置（2026-09-16 实机重大 bug）**：`cv2.minMaxLoc` 返回 `(minVal, maxVal, minLoc, maxLoc)`，`minLoc` 是**第 3 个**。`core/alignment.py` 原写作 `mn, _, _, ml = cv2.minMaxLoc(res)` ⇒ **分取最小值、点取最大值**，于是"分很漂亮、投影落在十万八千里"（最大代价点 ≈ 参考图的全黑边）。
  - 实机 09-16 批 6 张：`overlap` 全在 **0.00~0.09**（用户报的"匹配到了很多时候没重合"），修后 **0.94~1.00**；eval 主集 overlap 闸 5/6 → **6/6**。`core/entrance.py` 的 class 档同款 bug 一并修（该档已删）。
  - **为什么回归用例一直没抓住它**：图标锚定档是窗搜 `radius=0`，响应面退化成 **1×1**，min 与 max 是同一个点 ⇒ 锚定路径天然免疫。离线 17.13/17.11 走的正是锚定，所以"渲染图验证通过"。真实机里锚点大面积出界（见下）→ 静默回退**全搜** → 一头撞上这个 bug。
  - 教训：**"离线通过、实机不行"时，先问"实机走的是不是另一条分支"**，别急着怀疑算法本身。
- **④ 手框样本路径曾 100% 死掉（2026-09-16 实机）**：墙重合口径必须有锚点（样本图标 ↔ 引索图标），而锚点要「图标在样本内的偏移」；手框路径原本没有样本原点这个入参 ⇒ `icon_off` 恒 None ⇒ 每个种子都弃权 ⇒ 手框**恒返回空**。日志里 **9/9 次**「手框样本仍无匹配」全是这个原因，**与框多大毫无关系**（那句"框小一点"的提示是误导，已改）。修法：`find_seed_by_entrance(..., sample_origin=(框左上角屏幕坐标))`。实测修后 8/8 有结果且与自动路径同种子。
- 模板=内容尺寸的「刀锋」：`int()` 取整可让 tw>rw → 尺度被跳过；`PAD=12` 黑边放宽可行域（坐标按 `cx0-PAD+ml` 补偿）。
- 游戏默认（不碰缩放）入口态 s≈0.40 在域外（2026-09-14 实测 6 局图标 k=1.0）：旧基线能匹配纯靠用户习惯性缩小地图（s≈0.9）偶然入域。救法=「图标当尺子·棘轮」：`_find_icon` 细网格（0.05 步）量出 k，`_crop_around_icon` 按 `max(1, k/icon_k_ref=0.44)` 放大裁样——**只放大不缩小**（缩小丢信息让小模板假获胜，17.13 实测 186px 裁样翻错种子）。s 与裁框大小无关，扩大取样梯子救不了缩放。
- 小模板假获胜：`MIN_TEMPLATE_PX=150`（真匹配模板世界覆盖 163-204 参考px）。裁样 half 上限 316（侧长 632×0.36=228 恰可搜；再大真尺度出域全'-'，710px 实测）。**已知副作用（2026-09-16 实测 17.11）**：裁样小的样本（208px，k≈0.45 的 8 月档）在真尺度 ~0.4 处 `min(tw,th)=83 < 150` 被整档跳过 ⇒ 真尺度**够不着**，只能在 s≥0.8 的错尺度上比。修它要先想清楚怎么同时保住"防小模板"——别只把 150 调小了事。
- **样本框大小不是"匹配不上"的变量（2026-09-16 实测，回答用户"是不是框太小"）**：12 张实机图 × half_frac∈{0.18(现网),0.25,0.32,0.45}，**结构占比几乎不随框大小变**（15%→15%→17%→17%；好样本反而 36%→36%→25%→16%，因为框大了会把更多雾圈进来）；同一张图被判「结构仅15%」而拒绝，把框放大 2.5× 还是 17%。**拒绝的原因是那片区域本身稀，不是框太小。** 另：被拒的样本里仍含 581~1348 个墙像素，而墙重合度量只需要墙 —— 见待办 5。
- 刚进门重雾样本（mask 8-15%）属信息缺失：快速失败闸拒答是正确行为，勿试图匹配（2026-09-14 晚 6 局入口图全此类）。eval_match 已镜像该闸：拒答单列、不计入 top-1/top-3 分母。
- 单类退化：样本 mask 内最大单类占比>90%（大片均匀走廊/雾）时，分类 SQDIFF「样本覆盖处类别全等」⇒ 多种子精确同分 0.000（2026-09-14 实测 98% cls3）。救法=扩大取样梯子（0.25→0.32，只在仍退化时降档、首个非退化即停——0.32 档会把并列翻成错误种子假干净）。好样本单类 ≤0.78。纯走廊画面对齐无解（信息缺失），3点标定兜底。
- 常驻屏幕检测干扰 NVIDIA 截图：跟随轮询每 tick 新建+销毁 mss 实例（GDI DC 开合 4次/秒）会让英伟达截图失效——`ui/capture.py` 必须常驻单例。**「投影隐藏期间不得截屏轮询」这句已于 2026-09-17 作废**：那个坑的成因是 DC 句柄反复开合，单例之后不再存在；现在地图被 G 关掉后投影虽隐藏但**仍以 1s 降频续轮询**（否则 G 开回来时无法自动恢复，见技术备忘⑤）。
- **地图开合判据 2026-09-17 换过**：开 ⇔ 侧栏导航列 NCC ≥ `NAV_NCC_MIN`(0.45) **或** 雾 ≥ `FOG_OPEN_MIN`(0.30)。**结构占比退出判定**（游戏世界实测能到 0.51，旧阈 0.10 会被跨过 ⇒ 判开误判）。阈值只在 `core/vision.py` 一处（`ui/follow.py` 的 `FOLLOW_FOG_THRESH`/`FOLLOW_STRUCT_THRESH` 已删，别再引入第二套）。**禁止 NCC** 那条是针对**匹配**的（原始像素 NCC 真种子进不了前6），导航列这里的 NCC 是「固定 UI 在不在」的二值检测，不是匹配打分，不冲突。
- 参考图与游戏内非逐像素一致 —— 但**「真种子分上限 ~0.10-0.13」这句是旧口径的产物，已作废，勿再引用**（见技术备忘①）。新口径下真种子的分**就是这次对齐的错配率本身**：实测真种子 0.022~0.129（= 2%~13% 像素类别不一致）、错种子 0.25~0.45。分差和绝对量纲都可直接读。
- 墙色已标定（2026-09-15，用户提供纯色块标样）：通路墙 BGR(138,119,111) 冷-27 亮119、房间墙 BGR(114,122,139) 暖+25、两墙交界 (108,108,115)；走廊地 (104,86,78)、房间地 (79,88,102)，墙地色距≥99。代码留存：`vision.WALL_BGR/WALL_TOL=48`，`classify_region` 出 cls5。墙类不进对齐层 SQDIFF 的真死因是**厚度惩罚**：游戏内墙 5-9px vs 参考墙 1-3px，直进重合分 0.046→1.31，参考侧膨胀±3px 也 1.3 级。（早先「两侧墙线画位不重合 6%/5%」的结论是**幽灵相位污染的误判**——用填充匹配的对齐当真值去测墙，错位 146px；图标锚定下墙重合 60-67%，素材与游戏内**同渲染器**。）**但入口层现在正是吃 cls5 的**（墙重合度量），那里墙是主体不是干扰项。
- **幽灵相位**（2026-09-15 用户圈图标破案）：迷宫走廊网格自相似，填充 SQDIFF 全搜可锁进错位 146px 的相位，分 0.046/overlap 0.80 双闸全过；锚点邻域给 4px 余量也会漂（墙重合 60%→4%）——景观平到任何余量都会被用掉。修法：`find_overlay_transform(hint_icon=(ref_cx,ref_cy,ix,iy))` 图标锚定**钉死平移只搜尺度**（`ANCHOR_RADIUS=0`），17.13 实测 score 0.046/ov 1.00/s 0.986/墙重合 60%。窗心在 ref 补边坐标系（原图坐标须减 cx0-PAD）。教训：好看的分+高 overlap 不等于对齐对——像素级验证须有独立锚点（图标）交叉。**待复测**：minMaxLoc 修好后全搜是否还会锁幽灵相位（见待办 6）。
- 跨侧亮度不可比：游戏内与参考图同位置亮度差 +20~40（通路 83 vs 61），且「亮冷≥68」类绝对阈值会把地面(~82)和墙(~119)一起抓成实心块（用户目测裁决）——任何按亮度切分必须逐图自适应且两侧同规，绝对阈值禁止。
- 入口：一楼 2 个(正门+侧门)、二楼 1 个，均带白箭头。正门固定最南+房形固定（分不出种子）；侧门/二楼位置随机、房形各异（主要判别依据）。
- 两段式对齐：M1 由「屏幕图标 ↔ 参考图图标({seed}_index.json)」+ 入口匹配尺度 s 构造（北朝上无旋转）；第二段 `find_overlay_transform(hint_s=s, hint_icon=…)` 图标锚定钉平移、全尺度扫 s 精修，不过闸回退全搜。`build_entrance_transform` 只用 s 与图标坐标，**不用 mloc**（mloc 那处 minMaxLoc bug 因此没影响 M1）。
- 投影窗已设 `WDA_EXCLUDEFROMCAPTURE(0x11)`：mss/热键全屏截屏/OBS 均拍不到投影（e1 实测 品红占比1.000→0.000），防自截污染。禁用 WDA_MONITOR(0x1)；换截屏后端需复验。设后不得再对该窗 `setWindowFlags`（重建 HWND 丢 affinity）。
- 图标锚定（2026-09-15 上线，`[match] anchor_pin`）：样本图标↔引索图标**重合后钉死平移**。依据 `experiments/e5_anchor_ab.py` 2×2 实测：**引索窗口放大到 2× 后全搜会炸**（主集 top-1 3/3→2/3、控制组低分确信 1→5——搜索域变大=自相似错位有机可乘），锚定把它抹平回 3/3 与 1，且控制组真种子排名 7~24 名→1~7 名。置 false ⇒ 入口层全体弃权；对齐层回退全搜。
- **锚定 2026-09-16 三处修正（重要）**：
  1. **尺度窄带（`s0±anchor_band`）已废弃**，锚定只钉平移、尺度走全域 `SCALES_ENT`。理由：`s0=r_fine/k` 的残差是 ~0.09 量级，±0.06 窄带兜不住 —— 实测它把 `verify_entrance_e2e` 的 17.11（真种子18）挤成种子15。`anchor_band` 键已删，锚点元组也不再带第三项 s0。
  2. **锚点出界 = 该种子弃权，绝不回退全搜**。旧写法是"本种子锚定一档都没落下 → 偷偷换全搜"，而全搜取每个尺度的全局最小、**恒 ≤ 锚点那一点的值** ⇒ "锚点用不了的种子"自动拿到更宽自由度，靠不公平优势压过真种子（17.11：种子15 锚点 0/13 档可用→回退全搜得 0.109，以 0.005 之差赢了种子18 的锚定值 0.114）。这与「锚定取分单调只升不降」是同一种病：**统计量不同就不可比**。修掉后主集 top-1 4/6→**6/6**。
  3. **代价（已知限制，实机比离线更严重）**：锚定要求模板完整落在引索裁图内，而**对齐层的模板是"面板内探明像素的包围盒"**，常常就是整个面板（1064×569，实测 100%）；侧门又在地图边界 ⇒ 图标锚定一摆就把模板顶出参考图边界。2026-09-16 实机 6 张：**可用档 4~12 / 25**，其中 180309 是 **0/25**（逐尺度全是"左越界/上越界"）。入口层同样有 11%~22% 的种子弃权。当前靠"锚定不过闸→回退全搜"兜住（minMaxLoc 修好后全搜是可靠的），但这是**隐式兜底**，见待办 6。
- **⑤ 地图开合判据 + 「G 关掉再开回来」（2026-09-17，用户报的两个跟随问题）**：
  - **「关了按 G 也不会再开」= 纯逻辑 bug**：`_follow_tick` 原本「投影不可见就早退」，把
    **跟随自动隐藏**（地图关）和**手动隐藏**（用户点按钮，明确的意图）混为一谈 ⇒ 关一次之后
    轮询全停，G 再也唤不回投影，只剩「点『显示地图』」和「按热键重匹配」两条路。修法：两种
    隐藏分开——自动隐藏**降频续轮询**（`FOLLOW_IDLE_INTERVAL_MS=1000`，每 4 格截一次），
    手动隐藏才停；且 G 开回来时**直接用上次烘焙好的整屏 RGBA 放回**（`_restore_overlay`，
    不重跑匹配，与关图前逐像素一致）。⚠️ 已知限制：关图期间地图**平移/缩放过**的话恢复的是
    旧位置（日志会提示按热键重匹配）——「投影跟着地图动」是另一件事（待办 6/8 那条线）。
  - **「按 G 关地图不灵敏」= 旧判据在真实游戏世界上误判为「开」**。逐张看图确认的负样本
    （游戏世界吊灯场景 结构**0.51**、木楼梯 0.21、走廊绿植 雾0.17、登录画面 结构0.52、
    结算、桌面）全被旧 `雾≥0.10 或 结构≥0.10` 判成「开」⇒ 地图关着投影还留着。
    **结构占比整个退出判定**，雾阈提到 0.30。
  - **新判据 = 侧栏导航列**（用户提议）：ESC / 导航至出口 / 门箭头圆钮 / + 圆钮 / 竖滑条，
    固定屏幕位置，模板 `assets/ui_nav_column.png`（`python build_ui_template.py <全屏实机截图…>`
    重建，多张取均值）。⚠️ **它是「高精度、低召回」，别指望它单独判关**，且低召回有确切
    原因——**布局**，不是「UI 淡出」：全屏实机（屏幕底行均值≈36，无任务栏）NCC 0.86~1.00；
    **窗口化**（底行≈220 是 Windows 任务栏）NCC 0.34~0.36，掉到闸下；查看器/桌面 0.03~0.16。
    邻域 ±40/±30 位置搜索**试过、没用**（17.11 定点 0.36→搜索 0.664，真负样本同时被抬到
    0.271，分离带反而更窄 ⇒ 不搜索）。已确认负样本定点 NCC 最高 0.271 / 可信正样本最低
    0.543 ⇒ 闸取 0.45。**NCC 高 ⇒ 判开可信；NCC 低不能判关，必须靠雾兜底** —— 窗口化时
    全靠雾兜底，这就是雾阈必须留着的原因。
    （副产品：`hotkey_20260916_1806xx`/`1714xx`、`08.26 17.11/17.13` 这些「真实截图」其实
    都是**窗口化**capture，屏幕底行有任务栏；`verify_follow` 用的 17.13/17.11 即此类。）
  - 全库 176 张对拍（`experiments/e30_map_open_gate.py`）：**旧开新关 14 张、旧关新开 0 张**；
    已知负样本 4/4 全判对；开→关→再开的完整状态序列在真截图上 7/7（`e32_follow_reopen.py`）。
  - 同一套判据也用在**热键路径的地图画面闸**（待办 3）。
- **⑥ 尺度 s 不能靠「对齐分」选出来（2026-09-17 实测；影响**每一次按热键**，与跟随无关）**：
  - **真值法（新工具）**：引索 json 存了入口白箭头图标在参考图里的 `cx/cy`，`_find_icon` 给它在
    屏幕上的坐标 —— 真对齐的 M 必须把前者映到后者（同一模板、同渲染器）。
    `err(s) = |M_s(cx,cy) − 屏幕实测图标|` 于是**逐尺度可问真值**：真尺度处是**尖谷**（实测 1~4px），
    两侧 30~800px。脚本：`experiments/e35_zoom_slider.py`（静态图扫描）、`e37_video_zoom.py`
    （用户录的缩放演示视频，逐帧标定）、`e36_scale_band.py`（对齐分 vs 尺度曲线）。
  - **对齐分对尺度是单调递增的，真尺度处不是极小**（实测同一帧：s=0.25→分 **0.079**、
    真尺度 0.35→0.091、0.40→0.145、0.50→0.216、0.65→0.357）。这是技术备忘①的「缩模板骗分」
    **发生在尺度轴上**：模板按 s 缩小 ⇒ 覆盖面积缩小 ⇒ 离群的不一致像素被一起缩掉 ⇒ 分必然更低。
    后果：「全域里挑对齐分最小的尺度」典型偏低 **−0.01~−0.02**，**曲线平坦时可偏低 −0.09**
    （= 投影错几千像素），且**恒被 `ALIGN_SCALES` 下界截住**（实测三帧返回恰好 0.3000）。
    同一批 4 帧上「挑分最小的尺度」只有 1/4 落在真尺度 ±0.005 内。
  - **真尺度范围比记的低**：游戏滑条走到最放大是 **s≈0.22**（圆点 y=382），最缩小 **s≈0.83**
    （y=752，2026-09-18 修正，旧记「最缩小 0.60」是**视频标定只覆盖到 y=708** 的错觉，见⑧）
    ⇒ 下界的 0.30 **切掉了放大态**。早先「实测视图 0.75~0.99」是旧素材/旧口径时代的数，**别再引用**。
  - **两条已标定的尺度来源**（2026-09-18 重做，见⑧）：(a) **右侧导航列缩放滑条的圆点 y**
    （用户 09-17 提示"长条+圆点，点的位置代表缩放程度"）——曲线 `vision.ZOOM_CURVE_C`，
    覆盖 y∈[375,755] → s∈[0.23,0.83]，精度实测 ±0.01，**全种子通用**；圆点越靠下 s 越大
    （`+` 钮在顶 ⇒ 往上=放大）。(b) **图标当尺子 `s ≈ 0.374/k`**（k 由 `_find_icon` 量，
    `vision.zoom_scale_from_k`）—— 5 个图标锚点真值上误差 ≤0.011，但 k 量化在 0.05 网格
    ⇒ 自身不确定度 ≈ `s·0.05/k`（s 大时可达 ±0.09）。
  - **滑条读数只在全屏布局可用**：窗口化时导航列整体位移（`nav_column_ncc` 掉到 0.34），
    读数失效 ⇒ 必须有回退路径（图标尺子即为此而生，它不依赖导航列）。
- **⑦ 自动跟随·第二步：投影跟着地图平移/缩放（2026-09-17 落地）** —— 旧行为是「地图一动就得
  再按一次热键」。设计在 `ui/track.py` 模块头，接线在 `app._follow_tick` → `_maybe_track` →
  `_track_tick`，离线回归 `experiments/e33_follow_track.py`（10/10）。
  - **不能重跑全搜**（1.5~2.0s，会冻死主线程）。走局部三档：**画面帧差闸**（面板降采样 64×34
    帧差 <1.2 灰度级 ⇒ 地图没动，1ms 直接跳过）→ **近处小窗**（上次位置 ±`TRACK_NEAR_R`=12px
    ref，**半径必须 < 半个迷宫格周期 25px**，这样窗内不可能有幽灵相位，小的平移漂移能稳稳纠回）
    → **单尺度全平移**（尺度取滑条值）。
  - **防幽灵靠「证据边际」不靠窗搜**：实测真位移时旧变换的分劣化 **3.6×/12×/4.2×**（新位置要好
    72~92%），而格内幽灵相对旧位置只好 **8~15%** ⇒ 规则 = 新分须 ≤ `TRACK_MARGIN`(0.60) × 旧分。
  - **尺度必须按优先级取滑条值，绝不"挑分最小的候选"**：分对 s 单调偏低（备忘⑥），滑条说 0.35、
    旧值 0.31 同场竞逐时挑分必选 0.31 —— 实测那样会偏 26px。**并且尺度变了就不许走小窗**：错
    尺度上小窗也能找到低分位置（实测假接受，图标偏 78~180px）。
  - **尺度源协议（2026-09-18 补，见⑧）**：滑条(1ms,±0.01) → 图标尺子(200ms,±0.05/k，只在滑条
    读不到时才量，且只在超出它自身精度时才覆盖旧值) → **两个都没有 ⇒ 不做全平移、就地判丢**。
  - **跟丢处置（用户选定）**：连 `TRACK_LOST_MAX`(2) 次判丢 ⇒ **隐藏投影**并清 `_last_rgba`
    （粘在错位置比空白有害），跟随不停（地图还开着，继续看着屏幕）。
  - **离屏实测（e33，判定用独立图标锚点真值）**：起点对齐是垃圾（偏 158/555/960/1179px）时，
    下一帧靠滑条尺度自动纠回 **1/7/2/3px**；视频缩放跟得上（14px/2px）；地图已关帧必判丢。
    **耗时**：帧差闸 1ms / 滑条读数 1ms / 近处小窗一次 ~113ms / 全平移 ~97ms / 图标尺子 ~200ms
    （参考侧预处理已按 ref 路径缓存，屏幕侧 69ms 无法缓存——模板必须来自当前帧，缓存会让平移
    被算成 0）。⚠️ 单线程主线程，拖动时每 250ms 有 ~110ms 占空。
- **⑧ 「只改缩放时投影不跟/不准」的两半病根（2026-09-18，用户 09-18 批四帧演示 + 报障）**：
  用户在 NVIDIA 目录放了 `11.07.13.59 → 11.07.23.86` 四帧，**只拖缩放滑条、不动地图**
  （种子12 侧门）。复刻链路跑出两个独立缺陷，都已修：
  - **一半①：滑条读数在最缩小态直接失败**。圆点能走到 **y=752**（真尺度 **0.83**，图标锚点尖谷
    5px + 图标尺子 0.87 双重确认），而旧检测窗 `NAV_DOT_RANGE=(380,730)` 与 `NAV_BAND` 高 740
    都够不着 ⇒ 峰-基只剩 4（不过闸）→ 读数返回 None。修：窗放宽到 [315,805]、`map_roi` 按新的
    `NAV_DOT_BAND_H` 加深 80px（**NAV_BAND 本身不动**，否则 nav_column_ncc 会 resize 模板、
    NCC 闸 0.45 与 e30/e31/e32 的标定全废——A/B 实测 e30 前后逐位一致 17/0/198）。
    ⚠️ 旧表在 y=708 处就给 0.60，**尾段偏低 0.23**（那个「最缩小 0.60」是视频只标到 708 的错觉）。
  - **一半②：滑条读不到时，跟随拿旧尺度去做全平移，然后**无条件接受**。实测：沿用 0.433
    （真值 0.83）搜到分 0.207/ov 0.99 → 过闸 → **图标偏 184px**。更糟的是**错尺度分更低**
    （真尺度 0.42 的全局极小 0.086，错尺度 0.30 反而 0.126、图标偏 **840px**）——技术备忘①的
    「缩模板骗分」发生在尺度轴上 ⇒ **没有任何分数闸能把「尺度错」拦下来**。修：滑条读不到就
    量**图标尺子**；两个都没有就**不做全平移、就地判丢**（小窗 ±12px 有界、仍需过 TRACK_OK，
    可以有），连丢 2 次隐藏投影。
  - **新标定**（`experiments/e46_slider_recalib.py`，75 张全屏帧 + 4 帧演示）：圆点 y → s 走
    `vision.ZOOM_CURVE_C`（三阶，`t=(y−560)/200`）。真值锚点 5 点（图标误差 1~5px）
    (382,0.230)(488,0.300)(589,0.395)(627,0.430)(752,0.830) + 图标尺子中位 7 点；拟合残差
    锚点 ≤0.015、尺子 ≤0.019。**读数精度实测 ±0.01**（四帧演示误差 +0.000/+0.001/+0.008/−0.004）。
  - **副产品：游戏缩放是绕玩家图标为中心的** —— 四帧里屏幕上的白箭头只挪了 40px 而尺度变了
    3.7 倍（(1097,486)→(1089,526)）。别据此做「无搜索传播」（锚点有个 ~40px 的漂移，像是
    撞到了地图边界钳制），但它解释了为什么小窗微调总能跟上。
  - **回归**：`experiments/e45_zoom_follow_replay.py`（3/3：滑条路径三步全 ≤5px、尺子路径
    ≤5px、无尺度源时一次全平移都不采纳且判丢 ≥2）。
- **⑨ 日志里的 `<` 会吃掉半句话（2026-09-18 bug②）**：`app._log_step` 用 `appendHtml`，
  `msg` 被当 **HTML** 解析 ⇒ 消息里任何 `<`（`导航列NCC-0.03(<0.45)`、`入口领先仅15%(<0.25)`）
  都被当成标签开头，**后面整段消失**。证据：日志里所有截断点都恰好紧跟一个 `<`；受害的正是
  「滑条为什么读不到」「入口领先闸阈值」这些最该看的诊断行。修法一行：`html.escape(msg)`。
  ⇒ 往日志/HTML 塞自由文本时先转义；**别用"日志没写全"当"代码没走到"的证据**。
- **窗口化 capture 的面板几何不可信（2026-09-17 实测）**：屏幕底行 ≈220（有任务栏）的那批截图
  里**游戏窗整体位移过**——实测 `hotkey_20260916_171738` 的标称面板内能看到「导航至出口」和
  缩放滑条（全屏布局下它们在面板**之外**）。⇒ 那批帧的对齐结果无意义，**别拿它们当对齐真值**
  （e38 早先「P0 只对 27%」就是被这批污染的：22 帧里 14 帧是窗口化）。全屏（底行<60）才作数。
- **对齐分选不出尺度 ⇒ 策略对比要用「图标锚点真值」（2026-09-17）**：`experiments/e38_scale_policy.py`
  在标注集上比「全域挑分最小（现网）/ 滑条窄带挑分最小 / 滑条窄带挑图标误差」，判定 = 过闸**且**
  图标误差<25px。结论：**滑条是比"挑分"更可信的尺度来源**（可验证的 4 帧上滑条 4/4 精确、
  挑分 3/4 近似）；但要用它把热键路径的收益证明出来，手上「对齐本身良定」的帧还只有 4 张
  ⇒ **热键路径的尺度窄带（原计划 1c）留待有测量再动**，本轮只把它用在跟随上。
  ⚠️ 2026-09-18 补充：滑条现在**读得准到 ±0.01 且覆盖全量程**（含最缩小 0.83），1c 的测量
  条件已具备；另外**最放大态真尺度 ≈0.22 落在 `ALIGN_SCALES` 下界 0.30 之外**（四帧演示第 1 帧
  实测），热键路径在那时会偏 ~40px（0.25:16px vs 0.30:41px）——1c 顺手能解掉。
- **实机复现结论（2026-09-16 晚，09-16 批 `captures/hotkey_20260916_18*`，共 12 张热键）**：
  1. 入口层墙重合表现很好：真种子分 50%/64%/72%/78%/80%/83%/92% 且领先次名 6~36 个点。
  2. 对齐层原本 6 张里 5 张 overlap 0.00~0.09 —— **根因是 minMaxLoc 取点 bug**（技术备忘③），不是度量也不是搜索。修后 0.94~1.00。
  3. 手框样本 9/9 次失败 —— 根因是手框路径缺 `sample_origin` ⇒ 锚点恒 None（技术备忘④），已修。
  4. 12 张里 6 张被「样本结构仅 7~19%」拒答。实测（见上面「样本框大小不是变量」那条）：**那些区域是真的稀**，与框大小无关。
- ~~**面板矩形右侧含游戏 UI（2026-09-16 实测）**~~ → **2026-09-17 复核：不成立，勿据此收窄面板**。
  全屏实机截图里导航列在面板**之外**（面板右缘 1732，列 1736~1900），开态图的面板掩膜列剖面
  在 cols 640~1064 全为 0。原结论是拿**照片查看器截图**测出来的（查看器缩放+标题栏位移，
  UI 列才会落进面板；`171453`/`224253` 那几批带「打开方式」标题栏和任务栏）。
  「假结构进 `_revealed_mask`」这个**机制**描述仍然正确，只是**触发条件是查看器/窗口化布局**，
  不是正常全屏游戏。参见待办 4。
- 热键路径**已有**「地图是否打开」的闸（2026-09-17，待办 3 done）：`_entrance_pipeline_impl`
  在 `detect_fog_panel` 后、跑匹配前调 `vision.map_is_open(shot, panel)`，非地图画面直接
  记日志返回。判据与跟随同源（导航列 NCC / 雾兜底，技术备忘⑤）。
- **eval 口径的坑（2026-09-16 加强版）**：主集 15 张里 **9 张被拒答**（`mask<20%` 快速失败闸），而 `top-1` 的分母**只算应答的那几张** ⇒ 头条数字显示 6/6，实际"按下热键后屏幕上出现的东西正确率"远低。评估真实体验必须把拒答和兜底显示都算作"没通过"。另外控制组的「确信=误报」会把**正确的**种子匹配也算成误报（2026-09-16 晚 4/11 里 3 例 `best==GT`，实际误报 1）——统计误报率时要按 `best==GT` 过滤，别直接读那个数。
- 拿老截图测匹配**不能用照片查看器**（2026-09-15 用户实测踩坑）：查看器会把图缩放到 85%/160% 并加标题栏位移，`panel.rect` 与真实尺度全对不上 → 真种子连前三都进不去，属预期而非算法退化。要测老截图走 `load_bgr` 直读文件（`eval/eval_match.py` 即此路径，或 `experiments/e5_render_match.py` 出图）。
- 像素级结论必须脚本测量，模型读图只判结构级对错。

## 工作规则
1. 一次任务一件事，prompt 含文件路径/函数名/验收命令/禁碰范围。
2. 验收只看脚本输出数字，不看「应该没问题」。
3. 大改先出计划，确认后动手。
4. 改崩了 `git checkout` 回滚缩小重来，不在原改动上修。
5. 阶段完成即 commit；长任务后 `/clear` 重开会话，靠本文件+计划文档恢复上下文。

## 不做（防范围蔓延）
- ✅ 自动跟随·第一步 done（e8f7f37；开合判据 2026-09-17 换过一次，见技术备忘⑤）：投影显隐跟随游戏内 G 开关。
  判定 = **侧栏导航列 NCC ≥ `NAV_NCC_MIN`(0.45) 或 雾占比 ≥ `FOG_OPEN_MIN`(0.30)**（结构占比已退出；
  阈值只在 `core/vision.py` 一处）。轮询：投影可见时 250ms；**地图被 G 关掉后降到 1s 续轮询**，
  G 开回来就用上次的变换把投影放回（不重匹配）；**手动隐藏才真停**（点『显示地图』resume()）。
  设置 `auto_follow` 默认开。复标/回归：`python experiments/e32_follow_reopen.py`（真截图开→关→再开 7/7）、
  `experiments/e30_map_open_gate.py`（176 张新旧判据对拍）。
- ✅ 自动跟随·第二步 done（2026-09-17，技术备忘⑦⑧）：**投影跟着地图平移/缩放**，不用再按热键。
  三档：画面帧差闸（没动就 1ms 跳过）→ 上次位置 ±12px 小窗（< 半格周期 ⇒ 无幽灵）→ 单尺度
  全平移（尺度按协议取，绝不挑分）。防跳错靠**证据边际**（新分须 ≤0.60×旧分；
  实测真位移改善 72~92%、格内幽灵只 8~15%）。跟丢连 2 次 ⇒ 隐藏投影 + 清旧图。
  **尺度源协议**（09-18 补）：滑条(1ms,±0.01) → 图标尺子(200ms,±0.05/k) → 都没有就**不做
  全平移、就地判丢**（错尺度上全平移会锁到偏 840px 的位置，且分比真尺度还低 ⇒ 没闸能拦）。
  **跟随心跳**（09-18 补，`app._track_beat`，限流 `TRACK_BEAT_MIN_SEC`=1.5s）：每 tick 写一行
  `跟随心跳: 帧差… 尺度源… s=… → 小窗/全平移/判丢 分… ov…`。立它的原因：实机日志 126 秒 ≈500
  个 tick 里 `投影跟着地图更新` **0 条**，而三条静默路径（帧差闸跳过 / 采纳但落在死区内 /
  `_maybe_track` 根本没被调到）在日志上完全同形 —— 没有心跳就只能猜（`跟随收口与UI改造计划.md` §4）。
  **G 关地图不许销毁投影**（09-18 bug①）：`_track_tick` 第一件事看本 tick 的 `is_open`
  （`_follow_tick` 传入的**原始**读数，比 `confirmed` 早半秒见到"关"），判关即 return（不跟踪、
  更不判丢）；`_track_lost(why, is_open)` 只在**地图确实开着**时清 `_last_rgba`（假丢留图、真丢清图）。
  旧版在 G 关闭动画帧导航列 NCC 掉到 −0.03 ⇒ 尺度源全失效 ⇒ 判丢 ⇒ 连丢 2 次隐藏投影并清图
  ⇒ 日志三段式 `19:41:32 跟丢 → 19:41:33 投影隐藏 → 19:41:58 上次没有成功投影`。
  回归：`python experiments/e33_follow_track.py`（10/10，判定用独立图标锚点真值）、
  `python experiments/e45_zoom_follow_replay.py`（3/3，09-18 四帧缩放演示）、
  `experiments/smoke_app_offscreen.py`（10/10）。
- ❌ 跟随·第三步（自动重匹配换种子）：跟踪只在**已确认的种子**上跟随；种子选错治不了
  （实测换错参考图后全平移仍能找到分<0.30 的位置 —— 显示闸挡不住错种子）。
- ❌ 噩梦 3 楼层+地下室（架构预留即可）
- ❌ UI 美化 / 打包发布
- ❌ 新匹配算法（NCC/SIFT/ORB/深度学习，均已验证或没必要）
- ✅ 多分辨率适配（代码 done，剩 C3 实测）：`panel_for_screen(w,h)`——1920×1080 精确=FIXED_PANEL、16:9 等比外推、config `[panel.rects]` 校准表、非16:9未校准拒绝。雾块/暗块动态检测已实测证伪，勿重试（测量在 `experiments/diag_panel_*.py`）。C3=真实非1920×1080 截图验证外推值+校准。
- ✅ eval 扩充（进行中）：标注集目标 30~50 张（**现 59 张** = 09-15 之前 25 张 + 09-15 批 5 张 + **09-16 批 29 张**；180413 用户记不清未标）。`eval/inbox/` 失败样本定期标注进 `labels.csv`。
  **`eval/labels.csv` 是唯一的真值存档** —— 拿到新截图先把真种子/入口写进去（列：`file,seed,floor,entrance_type,is_entrance_view,notes`），不要每次重问；`is_entrance_view=1` 是主集（刚进入口）、`notes` 里写 `0` 是控制组。
- **待办（按杠杆排序）**：
  1. ✅ **兜底显示骗人** done（2026-09-16 晚）：不确信/对齐没过闸时**一张图都不显示**（`app.py::_refuse`），并把 `auto_align_overlay`（那个按 `min(pw/cw, ph/ch)` 把参考图**铺满面板**的渲染器）连同 `_show_centered` 整函数删掉 —— 用户报的「乱给一张素材、比例都不对」就是它。种子ID仍写状态栏/日志；要看参考图请显式走「▶ 按此种子对齐」或「✋ 手动选点重合」。
      ⚠️ 取舍：失败时**连上一次的成功投影也一起隐藏**（避免旧位置被当成新位置）。若实机觉得"宁可留着旧的"，改成"保留不动"只需一行。
  2. ✅ **eval 换口径** done（`experiments/e26_e2e_truth.py`）：分 出图·对 / 出图·错 / 藏图 / 拒答 四类，并同时算"放开结构闸"的反事实。`eval_match.py` 头尾已加警告：分母只算应答的。
  3. ✅ **热键路径加 `map_is_open` 闸** done（2026-09-17）：`app._entrance_pipeline_impl` 在
     `detect_fog_panel` 之后、跑入口匹配之前判地图画面，非地图直接记日志返回（不动投影）。
     判据与跟随同源（导航列 NCC / 雾兜底）。⚠️ 代价：**窗口化/照片查看器里跑不出结果**——
     那种布局下导航列不在标定位置、雾也可能低，会被判「不是地图」。这是**正确的拒绝**
     （离线拿查看器截图测匹配本来就不算数，见下面那条坑），但别误当成算法退化。
  4. ✅ **面板右侧游戏 UI 带 —— 结论：不存在，无需修**（2026-09-17 复核）。全屏实机布局下
     导航列在**面板之外**（面板右缘 1732，列在 1736~1900），实测开态图的面板掩膜列剖面在
     cols 640~1064 **全为 0**（`hotkey_20260916_221826/222657`）。2026-09-16 那条「面板右缘
     切进导航列、污染 2%~25% 掩膜」是**拿照片查看器截图测出来的**——查看器把画面缩放位移，
     UI 列才会落进面板（`171453`/`224253` 那几批带「打开方式」标题栏和任务栏，已逐张验）。
     故收窄 `FIXED_PANEL` **不要做**：会把真地图边界切掉。
  5. ✅ **样本快速失败闸：加「入口领先幅度」逃生门 done（2026-09-18）**。用户问「样本对入口探明
     程度的要求是不是太高了」—— 是，而且有**严格支配**的改法。
     - 原判据（`sample_mask_min`=0.20，非雾结构占比）量的是「非雾**面积**」，而入口层判的是**墙**
       （被拒样本里仍含 581~1348 个墙像素）⇒ 它只是"能不能判"的**代理**。
     - **待办 5 旧结论「别做」只对"完全拆掉闸"成立**：那确实 +9 对/+13 错。但换成「结构不足时，
       若入口 top1 **领先次名** ≥ `lead_min` 则放行」，实测（`experiments/e34_evidence_gate.py`
       44 张主集扫档）：**领先≥0.25 → 出图且对 14→21（+7）、出图但错仍是 3 张没多**；领先≥0.20
       开始多 1 张错的；≥0.15 崩（错 3→10）。
     - 落盘：`config.toml [match].lead_min`；`app._lead_frac` + `_entrance_pipeline_impl`；
       `eval/eval_match.py` 与 `experiments/e26_e2e_truth.py` 镜像同一口径。
     - **实测（e26 真实口径）**：出图且对 **14→21/44 = 32%→48%**，出图但错 3→3，拒答 27→20。
       `eval_match` 应答 top-1 14/17→21/24、拒答 27/44→20/44；控制组 4/11 不变（**没有多放错图**）。
     - 已知留白：21.59.28.73（领先 15%）与 21.50.44.04（22%）两张**本来能判对**的仍在拒答里
       —— 要救它们就得把门槛降到 0.20 以下，代价是多放错图（见上表）。
  6. **锚定还要不要 — 优先级已升** → 入口层是瓶颈（48%），而锚定在入口层的代价正是"11~22% 的种子弃权"，实机对齐层更糟（4~12/25 档可用、180309 是 0/25）。minMaxLoc 修好后全搜可能已可靠（见技术备忘③）。需先量：① 全搜现在还会不会锁幽灵相位（对 09-15 那批 146px 案例重测）；② 全搜 vs 锚定在 59 张标注集上的 top-1 对拍。若全搜可靠 ⇒ 入口层与对齐层的锚定机制 + "锚点出界即弃权"整套可简化掉，**待办 8 的大半收益不必等它**。
  7. 🟡 **显示闸已做一半**：0.25 → **0.30**（2026-09-17 落盘）。依据：172012/180442 两张**种子判对**的只因分 0.263/0.282 被藏图；放宽后出图且对 12→14、**错的仍是 3 张没多**（那 3 张错图的分反而只有 **0.124** —— 这个闸本来就**挡不住错种子**，它只判"几何放得下"）。剩下一半 = **入口闸 0.45 改看领先幅度**：180203 种子判对却卡在 0.496 只算"不确信"；wall 口径下真种子领先次名的分布还没量过。
  8. **把墙重合用到对齐层（潜在最大收益）**：幽灵相位的根源是"5 类掩膜的分数景观太平"，而墙是尖的 ⇒ 若对齐层也换成墙重合，图标锚定可能**不再必要**，顺带解掉待办 6 的死结。需先在 e2e/eval 上量，别直接上。
  9. ✅ **09-16 批真值入表** done（29 张；`eval/labels.csv` 现 59 行）。180413 用户记不清，未标。
  10. **热键路径的尺度窄带（原计划 1c）—— 测量条件已具备（2026-09-18）**：滑条读数现在准到
      ±0.01 且覆盖全量程，把它折成 `s_ui ± 0.05` 传给 `find_overlay_transform(scales=…)` ⇒ 全搜
      25 档 → ~5 档（1.5s → ~300ms），同时消掉「尺度平坦」错误源，并解掉**最放大态真尺度
      0.22 < `ALIGN_SCALES` 下界 0.30** 的截断（实测偏 40px）。**会改热键路径行为，必须先对拍**
      （59 张标注集上比 窄带 vs 全域 的 top-1/拒答/出图且错，劣化即撤）。
  11. **跟随收口 + UI 改造（2026-09-18 立，`跟随收口与UI改造计划.md`）**：T1 收口跟随 ✅done
      （心跳日志 + bug①②，见技术备忘⑨与「自动跟随·第二步」末段）；**T2** UI 折叠（三个手动步骤
      → `⋮ 更多` 菜单，`app._show_options_menu` 已有雏形）；**T3** 换种子 + 会话内黑名单
      （入口层 top-1 只有 ≈48%，取 `find_seed_by_entrance` 的 top3 顺位重跑对齐；黑名单按热键清空）。
      ⚠️ **T1 的心跳日志要先拿实机数据**（全屏进图 → 拖缩放 → 回读 `跟随心跳:` 列）再动阈值。
- ✅ 重构收尾验收（进行中）：游戏内实测 10 局有记录（置信/兜底成功率、重合观感）后重构才算完成。

## 运行
```
cd "D:\claudecode out"
python app.py                       # 主程序（悬浮窗 UI + 热键分发）
python eval/eval_match.py           # 旧口径（分母只算应答的，头条数字偏乐观）
python experiments/e26_e2e_truth.py # **真实口径**：按下热键后屏幕上出现的东西（出图·对/错/藏图/拒答）
python eval/verify_follow.py        # 对齐原语回归（基线 6/6）
python eval/verify_entrance_e2e.py  # 入口匹配+对齐端到端回归（主集 17.13✓ 17.11✓）
python experiments/e33_follow_track.py    # **跟随·第二步回归**（投影跟地图平移/缩放，10/10）
python experiments/e45_zoom_follow_replay.py # **缩放跟随回归**（滑条/图标尺子/无源三条路，3/3）
python experiments/smoke_app_offscreen.py # 离屏冒烟（投影窗/跟踪接线，10/10）
python build_entrance_index.py [种子号 ...]  # 重建入口引索
python build_ui_template.py <全屏实机截图 ...>  # 重建导航列模板 assets/ui_nav_column.png
python calib_annotate.py <截图路径>  # 校准标注图
```

## 口径
- 入口层 = 墙重合（`1 − 双向覆盖率`，闸 `score_confident = 0.45`）。
- 对齐层 = 错配率（闸 `align_score_max = 0.30`，2026-09-17 由 0.25 放宽）。
- 两层都「越小越好」，但**量纲不同**，闸不能互换。旧口径（入口 class / 两层 sqdiff）已删，回滚走 git。

## 诊断脚本（在 `experiments/`，出图在 `experiments/out/`）
> ⚠️ `experiments/` 在 `.gitignore` 里（一次性脚本，不入库）—— **这些脚本只存在于本机工作副本**。
> 换机器/清目录后需重写；下面记的是"要诊断这类问题时该造什么工具"。
| 脚本 | 用途 |
|---|---|
| `e9_live_compare.py` | 实机截图四行对比图（游戏 / **app 实际显示的铺满版** / 两段式对齐 / 样本↔引索） |
| `e10_anchor_audit.py` | 锚定分支审计：逐尺度为何被跳过、锚定 vs 全搜 vs 兜底 |
| `e11_gt_audit.py` | 真值审计：真种子 vs top1 种子的对齐分数对比 + 叠图 |
| `e12_cls_confusion.py` | 真对齐下的类别混淆矩阵（一致率 / 谁在贡献分数） |
| `e13_metric_anatomy.py` | 度量解剖：diff² 均值 vs min(diff²,1) vs 不一致率 |
| `e14_newmetric_trial.py` | 换口径试算·定种子层（真种子排名 前/后） |
| `e15_align_newmetric.py` | 换口径试算·对齐层（新口径选中 vs 现网选中 + 叠图） |
| `e16_1711_anchor.py` | 单样本锚定诊断（逐尺度锚定分/全搜分、可用档数） |
| `e17_perf.py` | 热键主链路耗时（入口匹配 / 对齐 锚定·全搜·fast）——**改匹配必跑** |
| `e18_metric_equiv.py` | 两种实现逐种子对拍（定位"等价却掉分"这类问题） |
| `e19_wall_overlap.py` | 墙重合可行性：真种子排名 + 位置敏感对照（挪 40px） |
| `e20_live_0916.py` | **一班一次**的实机批审计：样本画像/入口/hand框/锚定可用档/全搜 vs 锚定 —— 定位 minMaxLoc 与手框死路用的就是它 |
| `e21_anchor_geom.py` | 逐尺度打印锚点落点与越界方向；反算全搜赢家落点（钉死"分与点不同源"） |
| `e22_ccorr_verify.py` | CCORR 响应 vs 手工逐位一致数对拍 + 手工粗扫找真最优（验证度量实现） |
| `e23_sample_size.py` | 样本大小 vs 结构占比 × 4 档（回答"是不是框太小了"） |
| `e27_ui_band.py` | 「侧栏 UI 带当地图开判据」首次测量：面板内外定位 + NCC 初值 |
| `e28_nav_column.py` | 导航列定点 NCC 在已标注开/关样本上的分布（含照片查看器对照） |
| `e29_closed_scan.py` | 全库扫描找关态样本：逐张 导航列NCC/雾/结构，标出关态候选 |
| `e30_map_open_gate.py` | **地图开合判据新旧对拍**（176 张）+ 已知负样本断言（改判据必跑） |
| `e31_nav_calib.py` | 导航列阈值标定：定点 vs 邻域搜索 NCC 的正负分离带 |
| `e32_follow_reopen.py` | **跟随开合链路回归**：真截图 开→关→再开 的状态序列（7/7） |
| `e34_evidence_gate.py` | **探明闸复核**：扫「结构≥0.20 或 入口领先≥X」找支配档（得 0.25） |
| `e33_follow_track.py` | **跟随·第二步回归**：静止帧差闸 / 真平移与缩放跟踪 / 地图关必判丢（10/10） |
| `e35_zoom_slider.py` | 缩放滑条标定①：定位轨道/检测圆点 + **逐尺度图标锚点扫描**（真值法） |
| `e36_scale_band.py` | **对齐分 vs 尺度曲线**：证明真尺度处不是极小 ⇒ 缩放不能靠挑分选 |
| `e37_video_zoom.py` | 缩放滑条标定②：拿用户录的缩放演示视频逐帧标定「圆点 y → 真 s」（**已被 e46 取代**：视频只覆盖到 y=708，尾段偏低 0.23） |
| `e38_scale_policy.py` | 热键路径尺度三策略对拍（全域挑分 / 滑条窄带挑分 / 窄带挑图标误差） |
| `e45_zoom_follow_replay.py` | **缩放跟随回归**：09-18 四帧「只改缩放」演示，复刻 `_track_tick`（滑条/图标尺子/无源三条路，3/3） |
| `e46_slider_recalib.py` | **缩放滑条重标定**：图标锚点真值 + 图标尺子（75 张全屏帧），产出 `vision.ZOOM_CURVE_C` |
| `e47_zoom_motion_gate.py` | **帧差闸测量**：连续缩放录像按 250ms 取帧对，证明拖缩放时帧差 ≥2.71（闸 1.2）⇒ 0.0% 被吃掉（2026-09-18，见`跟随收口与UI改造计划.md` §2.2） |
| `smoke_app_offscreen.py` | 离屏冒烟：面板适配 + 跟随两步的 app 接线（不注册热键、不进 exec） |
