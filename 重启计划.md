# 加页手记 · 摸金地图叠层工具 — 重启计划

最后更新：2026-09-08（讨论结论，待执行人亲自检查）

## 背景
项目是《第五人格》摸金模式的地图叠层辅助工具。旧版已积累 ~4243 行 / 36 个 .py，但核心"自动识别种子+精准对齐"在真实游戏中不可靠，且堆了大量一次性诊断脚本与废弃原型。本文档是 2026-09-08 讨论后定下的重启方案：**只保留有用部分，坚决清理乱七八糟的代码**。

旧版状态详见"D:\claudecode\jiaye-map\项目说明.md"（保留作历史，但其"已实现"清单描述的是旧版整图路线；以本计划为准）。

---

## 一、锁定决策

### 1. 识别(identification) = 入口引索匹配
- 游戏内入口部分截图 ↔ 参考图(答案地图)入口部分的引索裁图，严格对比定种子。
- **放弃整张地图对比**（迷雾污染、低探明无解、渲染差异）。
- 已有能力：自动检测参考图入口图标并裁引索（`build_entrance_index.py`）。
- 诚实边界：仅在「刚进入口 + 入口图标在视野 + 侧门/二楼（正门形状固定分不出）」时可信。这是设计使然——触发时机就是玩家最自然想用工具的时刻。
- **评估集必须用"刚进入口"样本，不要用随机抽样**（否则严重低估召回）。

### 2. 对齐(alignment) = 两段式 + 手动兜底
- **第一段**：入口引索匹配本身给出初始变换。引索匹配已检测到入口图标屏幕位置，builder 已把各种子入口图标参考图坐标存进 `{seed}_index.json` → 屏幕↔参考图 + 匹配尺度 = 缩放+平移变换。需小改 `find_seed_by_entrance` 让它把内部 (尺度,位置) 吐出来。
- **第二段（可选精修）**：用 `find_overlay_transform` 在第一段的尺度 hint 附近做局部精修（它已支持 `hint_s`）。
- **兜底**：入口图标检不出 → 手动 3 点标定（`affine_from_points`，3 下点击必对）。
- 地图恒北朝上，**不用旋转，只要缩放+平移**。

### 3. 连续跟随(auto-follow) = 推迟
旧版那套三级降级 + 亚像素 + 切层状态机**从没在游戏内实测过**。重启先证明一次性对齐可靠，再做跟随。别再往没实测的状态机上堆复杂度。

### 4. 测量闭环
单一评估 harness（以 `eval_match.py` 为起点），配置化路径，标注"刚进入口"样本集，输出 top-1/top-3 准确率。每个改动对着它跑。

---

## 二、保留的（有用部分）

### 干净模块，几乎原样保留
- `map_library.py` — 参考图库（28种子×2楼层，文件名解析建索引 + clue 反查）。干净可复用。
- `overlay.py` — 透明点击穿透悬浮层。
- `capture.py` — 屏幕捕获。
- `hotkey.py` — 全局热键 Ctrl+Shift+F。
- `build_entrance_index.py` — 入口引索构建（自动检测参考图入口图标、正门=最南、裁引索+标注+JSON）。
- `entrance_match.py` — 入口引索匹配（**小改：返回内部 尺度/位置 供对齐**）。
- `manage_materials.py` — 素材增删改 UI。

### 要抽出来 / 重定位的（从 god module 摘出）
- **`vision.py`（新，从 `matcher.py` 抽）**：`classify_region`、`FOG_BGR`+迷雾剔除、`content_bbox`、`load_bgr`、`_find_icon`(in-game 图标检测)、`FIXED_PANEL`、`detect_fog_panel`、`map_is_open`。这些是共享视觉原语，引索路径/对齐/校准都依赖它，不再埋在 400 行 god module 里。
- **`alignment.py`（新，从 `find_seed_submap.py` 抽）**：`find_overlay_transform` + `_revealed_mask`/`_match_at_scale`/`_subpixel_min`/`SCALES`/`ALIGN_SCALES`/`PAD`。它是**对齐引擎，不是 identification**，不能跟整图匹配一起扔。
- `map_to_overlay_rgba`、`auto_align_overlay`、`affine_from_points`、`transform_point`（从 `matcher.py` 摘）→ 进 alignment 或 render。

### 数据资产（保留）
- `_icon_entrance.png` — 白箭头入口图标模板（由 `20.01.15.92.png` 裁出）。
- `entrance_index/` — 各种子入口引索裁图 + `_index.json` + 标注图（28种子，**种子28一楼侧门缺，需补**）。

---

## 三、坚决不要的（乱七八糟，坚决删）

### 一次性诊断脚本（12个，全删）
`_diag.py` / `_diag2.py` / `_diag3.py` / `_render_cmp.py` / `_zoom_cmp.py` / `_metric.py` / `_metric2.py` / `_verify_score.py` / `_verify2.py` / `_verify3.py` / `_verify4.py` / `_verify5.py`
（全是写死同一张 `17.13.06.98` 截图/种子18 比对 metric 变体的 scratch，无任何模块依赖它们。）

### 废弃早期原型（4个，全删）
`orb_match.py`（ORB 特征匹配原型）/ `analyze_match.py` / `auto_match_experiment.py`（28种子×多尺度模板实验）/ `find_seed_loc.py`（旧 SIFT+RANSAC 定位，被 `find_seed_submap` 取代，且带模块级可变全局 `H,W`）

### 被放弃的整图 identification（删函数）
- `matcher.py` 里 `find_seed`、`find_seed_color`（整图亮度/颜色匹配，被入口引索路线取代）。
- `find_seed_submap.py` 里 `find_seed_submap`（整图子图 identification；**保留同文件的 `find_overlay_transform`，先挪走再删**）。

### 要重写而非保留的
- `app.py`（574行 god file）：保留 UI 外壳 + 热键分发；重做状态/线程（follow 推迟，先不建复杂状态机）。手搓 `threading.Thread`+`Signal` 改正经 worker（`QThread`/命令队列）或暂单线程。
- `matcher.py`（400行 god module）：拆成 `vision.py` + 其余归位，**不整体保留**。
- `calib_annotate.py`：保留校准工具，但删掉它复制的第三份 `classify_region`/`_find_icon`，改 import vision 核心（消除 drift）。

### 散落评估脚本（整合，不原样保留）
`eval_match.py`（作为单一 harness 起点，改造：配置化路径 + "刚进入口"标注集）；`scan_shots.py` / `scan_entrance.py` / `verify_align.py` / `verify_follow.py` / `verify_entrance_e2e.py` / `check_icon.py` 的有用逻辑并入 eval 或退役。

### 可删小工具
`view.py`（无依赖，ASCII 看图，纯调试）/ `capture_screen.py`（可并入 calib 或 capture）— 可选删。

---

## 四、资料位置（重启时改为配置驱动，别再焊死）

| 资料 | 当前路径 | 说明 |
|---|---|---|
| 参考图库（素材/答案地图） | `D:\Pictures\20260818摸金地图` | 28种子×2楼层，手绘标注参考图，文件名 `{种子} {方向}-{门特征}{楼层}.png` |
| 游戏截图库（测试样本） | `D:\Videos\NVIDIA\IdentityV` | NVIDIA 录制游戏截图，评估/扫描用 |
| 手绘标定原图 | `D:\Pictures\Camera Roll\` | 用户自绘标注参考图来源 |
| 入口引索 | 项目内 `entrance_index/` | `{seed}_正门/_侧门/_二楼.png` + `_index.json` + 标注图 |
| 图标模板 | 项目内 `_icon_entrance.png` | 白箭头入口图标 |
| 运行时截图 | 项目内 `captures/` | 热键触发捕获 |
| 校准/检测产物 | 项目内 `校准输出/`、`图标检测检查/` | 可重建 |

常量（重启 → 配置）：
- 固定面板坐标(1920×1080)：`(668,166,1064,569)`
- 迷雾色 BGR：`(58,47,37)`（注意：冷色不是中性灰，见备忘）

---

## 五、重启步骤建议（准备，先不写代码）

1. **先 `git init`**（当前不是 git 仓库）——每个重构/实验一个 commit/branch，throwaway 脚本变历史而非常驻文件。
2. **建 `requirements.txt`**（依赖：opencv/numpy/Pillow/PySide6/mss/pywin32，当前无依赖清单）。
3. **建配置文件**（如 `config.toml`）：所有路径、面板坐标、迷雾色、阈值外置。
4. 按本计划做「删 → 抽 → 重定位」：先删 12+4 个废文件，再抽 `vision.py`/`alignment.py`，再改 `entrance_match` 返回对齐信息。
5. **评估集**：标一批"刚进入口"截图(种子已知)，接入 `eval_match`，出基线 top-1/top-3。
6. 一次性对齐(entrance → hint → refine → 手动兜底)在游戏内实测可靠后，再考虑跟随。

---

## 六、关键技术备忘（从旧项目说明摘的有用坑，带过来）

- **迷雾是冷色不是中性灰**：BGR(58,47,37) 暖度 -21，`classify_region` 会误判成通路(cls=3)。任何 `revealed = cls∈{1,2,3}` 的地方都要额外按颜色距离(`FOG_BGR` tol24)剔除迷雾，否则迷雾当通路污染模板。
- **尺度搜索要细**：`SCALES` 0.05 步长(0.4-1.0)；真实面板适配尺度~0.7-0.85，粗档(0.4/0.6/0.8/1.0)会擦肩→大模板错位、分虚高。别改回粗尺度。
- **别换 NCC**：实测原始像素 NCC 真种子进不了前6，5分类 SQDIFF 更优。保留分类法。
- **overlap 闸是必备二次验证**：低分可能侥幸(小模板/小掩膜)。`find_overlay_transform` 的 overlap(已探明逆变换后落在参考真实内容上的比例) ≥0.4 才算可靠。置信 = 分<0.10 且 overlap≥0.4。
- **地图恒北朝上**（用户确认）：匹配/对齐无需试旋转。参考图亦北朝上。
- **入口规则**：困难模式一楼有 2 出入口(正门+侧门)、二楼 1 个(二楼入口)，每个都有白箭头图标。正门固定最南边、房间形状固定（分不出种子）；侧门/二楼位置随机、房间形状各异（主要判别依据）。
- **warpAffine 语义**：默认(不加 `WARP_INVERSE_MAP`)传的就是 src→dst 正向矩阵，内部自动求逆采样。`map_to_overlay_rgba` 直传 `find_overlay_transform` 的 M 即正确；传 M 的逆会投到屏幕左上角外（踩坑，勿再试）。
- **模板=内容尺寸的"刀锋"**：玩家缩放到"整图刚好适配"时，模板尺寸≈参考内容尺寸，`int()` 取整可能让 tw>rw → 尺度被整体跳过。`find_overlay_transform` 用 `PAD=12` 黑边放宽可行域（坐标推导按 `cx0-PAD+ml` 补偿）。
- **"合成面板"测不了匹配器**：把参考图按已知变换投影成面板来测对齐——不可行，无法复刻"游戏原生渲染 vs 手绘参考"噪声关系。测跟随/对齐用真实数据（同局两截图互为平移真值，如 17.13→17.11 Δt=(55,222)）。
- **参考图与游戏内非逐像素一致**：真种子匹配分上限~0.10-0.13（非~0.02），但与错种子分差够大仍能区分。渲染略有差异是正常的。
