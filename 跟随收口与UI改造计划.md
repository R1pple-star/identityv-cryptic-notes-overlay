# 跟随收口 + UI 改造计划（2026-09-18 立）

> 给下一个会话的交接件。**先读本文件，再按需读 `CLAUDE.md` 的对应段落。**
> 上一轮（`7818bc4`）已修完"滑条标定 + 尺度源协议"，本文件记的是**它之后**的新发现与新计划。

## 0. 为什么要立这份文件（用户原话）

> 「是新进程，全屏。日志在 `D:\Desktop\错误日志.txt`。应该就是有bug；另外因为现在自动效果变好了，我们再改一下ui将那三个手动步骤放到类似选项的按钮里让面板更简洁。另外我希望加一个换种子的选项，如果种子没对可以更换，并把错种子放到一次性的黑名单里让这次匹配换种子中不会再出现，当然按"自动匹配"时就把黑名单重置防止匹配不到种子。」

即三件事：**① 跟随有 bug 要收口 ② UI 折叠三个手动步骤 ③ 换种子 + 会话内黑名单。**

## 1. 已经做完的（`7818bc4`，勿重做）

- 缩放滑条重标定到全量程：`NAV_DOT_RANGE=(315,805)`、`NAV_DOT_BAND_H=820`（只加深读数裁切，
  **`NAV_BAND` 的 740 不动**，否则 `nav_column_ncc` 会 resize 模板、NCC 闸 0.45 与 e30/e31/e32 全废）；
  曲线 `vision.ZOOM_CURVE_C=(-1.042, 0.48454, 0.20267, 0.22509)`，`t=(y−560)/200`，y 钳 `[375,755]`。
  4 帧演示读数误差 **+0.000/+0.001/+0.008/−0.004**。
- 尺度源协议：**滑条(1ms,±0.01) → 图标尺子 `s≈0.374/k`(200ms,±0.05/k) → 两个都没有 ⇒ 不做全平移、就地判丢**。
- 回归：`e33` 10/10、`e45` 3/3、`smoke` 8/8、`eval_match` 21/24 + 拒答 20/44 + 控制组 4/11、`verify_*` 6/6、17.13✓/17.11✓、e30/e32 不变。

## 2. 本轮新增的测量（关键数据，别再重量一遍）

### 2.1 「无极缩放」已在**独立录像**上确认
`D:\Videos\NVIDIA\IdentityV\IdentityV 2026.09.14 - 14.53.02.02.mp4`（954 帧 @60fps = 15.9s）：
583/954 帧读得到滑条，**s 单调 0.826 → 0.230**，无平台无跳档。

| 端 | 圆点 y（模板内） | 真尺度 s |
|---|---|---|
| 最放大 | 382 | **≈0.23** |
| 最缩小 | 752 | **≈0.83** |

动态范围 **3.6×**；比例是 `ln s = 三阶多项式(t)`，**非等差非等比**（见 `vision.ZOOM_CURVE_C` 长注）。
⇒ 最放大态真尺度 0.22 落在 `ALIGN_SCALES` 下界 0.30 之外 ⇒ **热键路径在那时偏 ~41px**（= 用户报的"匹配的缩放也不完全对"），归**待办 10 / 计划 1c**。

### 2.2 「画面帧差闸」已排除（`experiments/e47_zoom_motion_gate.py`）
按 `FOLLOW_INTERVAL_MS`(250ms) 的帧间隔取帧对，算面板 64×34 帧差（与 `app._track_tick` 同式）：

| \|Δs\| 每 250ms | 帧对 | 帧差中位 | 帧差 < 闸 1.2 的占比 |
|---|---|---|---|
| = 0（没缩放） | 198 | **0.03** | 100%（闸正确关着 ✓） |
| ~0.004（刚过死区 0.004） | 21 | **2.71** | **0.0%** |
| 0.02~0.05 | 121 | 5.93 | 0.0% |
| 0.05~0.10 | 105 | 11.68 | 0.0% |
| 0.10~0.20 | 60 | 10.98 | 0.0% |
| 0.20~1.00 | 48 | 13.26 | 0.0% |

**只要尺度动了，帧差最小 2.71（闸的 2.3 倍）；334 对里被吃掉 0.0%。** 09-18 四帧演示同样全过闸
（Δs 0.071/0.127/0.398 → 帧差 11.6/10.3/11.3）。⇒ **`TRACK_MOTION_MAD` 不是"投影不动"的元凶。**

### 2.3 实机日志（新版 + 全屏，19:36–19:43）的一条硬事实
`D:\Desktop\错误日志.txt`：

| 事件 | 次数 |
|---|---|
| 热键按下（`截屏 OK`） | 15 |
| 成功投影（`投影已显示` / 手动重对齐） | 8 |
| G 关 / 开 | ~10 / ~9 |
| **`跟随: 投影跟着地图更新（…）`** | **0** |
| `跟丢（…）` | 1 |

"地图开着 + 投影显示"的累计时间 ≈ **126 秒 ≈ 500 个 tick**（19:39:14–19:39:43、19:39:55–19:40:07、
19:40:20–19:40:26、19:40:40–19:40:46、19:40:59–19:41:18、19:41:20–19:41:29、19:41:31–19:41:33、
19:42:05–19:42:15、19:42:18–19:42:44、19:42:46–19:42:47、19:43:17–19:43:20、19:43:50–19:43:53）。

**关键推理**：代码里唯一不写日志的成功路径是"采纳了、但 `advanced()` 死区不过（位移 ≤1.5px 且相对尺度变化 ≤0.004）"
——那本来就不需要动投影。**即：只要地图动了 >1.5px 且跟随采纳了，一定会留下日志。日志里一条都没有。**

### 2.4 实机日志**第二份**（22:43–22:52，T1 之后的版本）——心跳本身有采样盲区
`D:\Desktop\错误日志.txt`（用户 09-18 晚重测，重启后的新进程 + 全屏）。表面症状：35 条心跳
**全是** `帧差0.00<闸1.2 画面没动 → 跳过`，看着就像"检测不到屏幕变化"。逐条核过之后：

| 核查项 | 结论 |
|---|---|
| 帧差公式在**真实 capture 路径**上对不对 | 对。`capture_region` 返回的是 `bgra[:,:,:3]`，**步长 4 的视图**（实测 `roi.strides=(4928,4,1)`），`cv2.resize` 与连续副本逐位一致（最大差 0）；`nav_column_ncc` 在同一视图上给 0.96 ⇒ 数据解释无误 |
| `region` 取的是不是面板 | 是。`map_roi(panel)=(668,166,…)`，切片 `[0:569, 0:1063]` 即面板本身 |
| 拖缩放时帧差该有多大 | e47 已测：**只要尺度动了最小 2.71**（闸 1.2 的 2.3 倍） |
| 那 0.00 是怎么来的 | **限流采样掉尖峰**。tick 250ms、心跳 1.5s ⇒ 每 6 个 tick 只记 1 个，而一次拖缩放往往就 1 秒（4 个 tick）⇒ 记到的那 1 个正好都在静止期。日志 22:46:01/:03/:05/:06 四条 0.00 全落在一段 8 秒窗口里，而同一窗口里 k 从 0.85 变到 0.6（真实缩放了 40%） |

⇒ **"帧差闸吃掉缩放"这条假说至今既没证实也没证伪**；能证伪它的数据在旧心跳里根本不存在。
另外同一份日志还抓到一条**真的** bug（见 §3 bug① 第二半）：

```
22:52:33 跟随: 地图开 → 投影已恢复（导航列-0.03偏低→雾0.41兜底）   ← 导航列根本不在（关态）
22:52:35 跟随心跳: 帧差4.30 尺度源无（导航列NCC-0.01(<0.45)，滑条读数不可用…）→ 判丢
22:52:35 跟丢（没有可信的尺度…）→ 已隐藏投影，请按热键重新匹配
22:52:42 地图已开，但上次没有成功投影 ⇒ 不自动恢复（请按热键匹配）   ← bug① 复发，入口是雾兜底
```

### 2.5 实机日志**第三份**（09-19 15:37–15:47）—— **破案：不是帧差闸，是一个 `NameError`**
用户重启进程 + 全屏 + 按热键 + 拖缩放/平移。§8 要的两件事都有了答案：

| §8 的问题 | 答案 |
|---|---|
| `本轮峰值` 有没有超过 1.2 | **有，而且大量**：2.51 / 3.62 / 3.98 / 4.34 / 4.86 / 4.90 / 5.73 / 5.86 / 7.73 / 8.28 / 8.45 / 8.63 / 8.76 / 9.35 ⇒ **帧差闸清白**，`TRACK_MOTION_MAD=1.2` 不用动（与 e47 的离线结论一致） |
| 有没有 `全平移 … 采纳` / `投影跟着地图更新` | **一条都没有**。`小窗`/`全平移`/`判丢`/`跟丢` 也全都没有 |

但 14 条心跳的 `／N tick` 相加得 **56 次「过闸」**（`_track_n` 只在过闸后才 +1），而
`_track_tick` 里**每一条过闸路径都写着 `force=True`** ⇒ 56 次跟踪尝试**一条日志都写不出来**。

⇒ 只能是主体在**写第一条日志之前**就断了。离线用**真代码**复现（`experiments/e48_track_tick_live.py`，
拿用户当场那批 `captures/hotkey_20260919_1541xx.png` 驱动真 `_maybe_track`）：

```
app.py:993  NameError: name 'TRACK_DEADBAND_S' is not defined
```

`ui.track.TRACK_DEADBAND_S` / `TRACK_OVERLAP_MIN` 与 `ui.follow.FOLLOW_MAX_CAPTURE_ERRORS`
**都没进 `app.py` 的 import 列表**。⇒ **跟随·第二步自 `1b888e7`（就是它自己那个 commit）起
一次都没运行过**，09-18 那份「126 秒 ≈500 tick / 0 条更新」也是同一个 `NameError`
—— §4 猜的三条静默路径**一条都不是**。

**为什么三个既有回归全没抓住**：`e33`/`e45` 是**复刻** `_track_tick` 的逻辑（各自
`from ui.track import …` 一份副本，名字当然齐全），`smoke_app_offscreen` 那句
`win._track_tick("不是 ROI", False)` 被 `is_open=False` 在**第一行**早退 ⇒
**真代码的跟踪主体从来没被执行过一次**。技术备忘③ 同款教训：
**「离线通过、实机不行」时先问「实机走的是不是另一条分支」**。

## 3. 三个**确定**的 bug（有日志证据，可直接修）
### bug ① 按 G 关地图会**销毁**投影（连带毁掉"G 开回来自动恢复"）
```
19:41:31 地图已开 → 投影已恢复
19:41:32 跟丢（没有可信的尺度（导航列NCC-0.03(     ← 你按了 G，这是关闭动画的过渡帧
19:41:33 跟随: 地图关 → 投影隐藏
19:41:58 地图已开，但上次没有成功投影 ⇒ 不自动恢复（请按热键匹配）
```
机制：开合状态机要**连续 2 帧（0.5s）**才确认"关"，而跟踪在这半秒里先撞上动画帧 → 导航列
-0.03 < 0.45 ⇒ 滑条读数失效 ⇒ 滑条/图标尺子都没有 ⇒ 判丢；而 `_track_lost`（`app.py:993-1003`）
顺手做了两件坏事：**隐藏投影** + **`self._last_rgba = None`** ⇒ 地图开回来时无图可放回。

**修法**：跟踪在地图判关时不做事（不判丢）；`_track_lost` 的"清 `_last_rgba`"只在**地图确认开着**时才允许。

**⚠️ 第二半（09-18 晚第二份日志才发现）**：光看 `is_open` 不够 —— 关闭动画帧里导航列 NCC
掉到 **−0.01**（列根本不在）而**雾 0.41 ≥ 0.30** ⇒ `map_open_from_roi` 照样判「开」（雾兜底的
代价），于是 `is_open=True`、判丢、清图，**bug① 换个入口原样复发**（22:52:35 → 22:52:42）。
所以"地图确认开着"必须以**主判据**为准：`map_open_from_roi` 现在返回第三项 `nav_ok`
（= 导航列 NCC ≥ 0.45 自己成立，不是雾兜底救的），一路传到 `_track_lost(why, is_open, trusted)`
—— 两个条件都满足才清 `_last_rgba`。

### bug ② `_log_step` 把 `<` 之后的整段吃掉
`app.py:370`：
```python
self.log_view.appendHtml(f'<span style="color:{color}">[{ts}] {msg}</span>')
```
`appendHtml` 把 `msg` 当 **HTML** 解析 ⇒ `导航列NCC-0.03(<0.45)，滑条读数不可用））→ 已隐藏投影…`
里的 `<0.45)…` 被当成标签吞掉，界面/导出里只剩残句。**证据：日志里所有截断点都恰好紧跟一个 `<`
的左括号后**（`…领先仅15%（`、`…导航列NCC-0.03(`）。受影响的消息：滑条失败原因、圆点峰不足、
入口领先闸阈值 —— 正是最需要看的那几条。

**修法**：`html.escape(msg)`（一行）。

### bug ③ 「按此种子对齐」按钮对齐不准（用户 09-19 报，同一条消息里）
```
15:40:00 手动重对齐: 南-三缺一门 重合0.05 重叠1.00 已投影
15:40:06 手动重对齐: 南-三缺一门 重合0.05 重叠1.00 已投影
15:40:14 手动重对齐: 南-三缺一门 重合0.05 重叠1.00 已投影
15:40:18 手动重对齐: 南-三缺一门 重合0.05 重叠1.00 已投影
15:42:43 / 54 / 57 / 59  …… 一模一样的 0.05 / 1.00
```
**8 次、4 分钟、分数与重叠一字不差** —— 中间地图已挪过好几处（同一份日志里 `入口图标`
从 (802,398) 走到 (1063,483)）。两处病根：

- **① 对齐的是旧图**：`app._realign` 写作 `if self._shot is None and not self._capture()`
  ⇒ 只在**从未**截过屏时抓一张，否则一直用**上一次热键那张**。同一张旧图 + 同一个变换
  ⇒ 分数必然逐位相同。投影于是落在「地图当时所在」的位置 = 用户看到的"不准"。
- **② 无锚点无尺度提示的全搜**：用户提的「把匹配时的对齐方式用在其他跟随和对齐的时候」
  正是正解。实测 `captures/hotkey_20260919_154124.png` 全搜得 **s≈0.39 / 分0.038 /
  重叠1.00（照样过闸）**，真值 s≈0.83（图标尺子 `0.374/k`，k=0.45）—— 技术备忘①
  「缩模板骗分」在尺度轴上的重演。

**修法**：当场截屏 + 复用 `_two_stage_align`（hint 精修 → 过闸即用 → 回退全搜），
`hint_s` 取**缩放滑条读数**（±0.01）、`hint_icon` 取 UI 入口下拉对应的引索图标。
入口选错 ⇒ 锚点错 ⇒ 过不了显示闸 ⇒ 回退全搜 = 旧行为，**不会更差**。

**验收**（`e48` 第 3 节，逐帧与**热键路径本尊**比参考图中心的落点）：

| 帧 | 旧（旧图+全搜） | 新（当场截图+两段式） | 热键本尊 |
|---|---|---|---|
| 154124 | 0px | **0px ✓** | 0.038/1.00 |
| 154201 | **31px** | **0px ✓** | 0.055/1.00 |
| 154208 | **160px** | **0px ✓** | 0.263/0.86 |

热键本尊那列与用户实机日志一字不差 ⇒ `_realign` 现在**等价于按热键**。

## 4. ~~尚未收口的问题：为什么 0 次更新~~ → **已收口（见 §2.5）**

> 下面这段是 09-18 的推理，**结论已被 09-19 推翻**：真正的答案是 §2.5 的 `NameError`
> （每次过闸都炸），三条"静默路径"一条都不是。留着是为了记住当时的推理错在哪 ——
> 在**没有心跳**的日志上，三种完全不同的原因确实同形，猜是猜不出来的。

三条**静默**路径在日志上完全同形：

1. 帧差闸判"地图没动" → `tr.note_ok(); return` —— **已在 §2.2 排除**；
2. 近处小窗采纳了，但位移/尺度变化在死区内 → 不重烘焙、不写日志（**合法**，本来就无需动）；
3. `_maybe_track` 这两行根本没被调到（`app.py:884-887`：`self._follow.confirmed is True` / `self._track.active`）。

第 3 条最可疑：`_show_overlay` 每次热键都 `_follow.reset()` 把 `confirmed` 清成 `None`，之后要**连续 2 帧同判**
才重新确立；而日志里那些**成对出现**的 `跟随: 地图开 → 投影显示`（19:39:55 / 19:40:59 / 19:41:31 /
19:43:17 / 19:43:50 各两条）正是 `reset()` 后重新 adopt 的痕迹——**说明 `confirmed` 在这套流程里确实反复归 None**。
（另一来源：`_restore_overlay` 内部走 `_show_overlay`，也会 `reset()`。）

**但"能不能"和"是不是"差一个观测** ⇒ 必须加**心跳日志**才能定论，不要再猜。

## 5. 任务计划（建议顺序 T1 → T2 → T3，每件一个 commit）

### T1 · 收口跟随（先做，改动小，全在既有文件）—— ✅ done（2026-09-18）
1. **心跳日志**（限流 1.5s，一行）：每 tick 写清 `帧差 / 尺度源(滑条|尺子|沿用) / s_ui / 动作(小窗|全平移|判丢) / 分 / ov`。
   位置：`app._track_tick`（`app.py:896`）。这是把 §4 收口的**唯一**办法。
2. **`_log_step` 转义 HTML**（`app.py:370`，bug ②）。
3. **G 关地图不销毁投影**（bug ①）：`_track_tick` 开头先看本 tick 的 `is_open`（由 `_follow_tick` 传入），
   地图判关时直接 return（不判丢）；`_track_lost` 不再清 `_last_rgba`，或只在"地图确认开着"时清。

**落地**（`TRACK_BEAT_MIN_SEC=1.5`，`app._track_beat`）：
- 心跳覆盖 §4 的三条静默路径，各自的原文：
  - `跟随心跳: 帧差0.03<闸1.2 画面没动 → 跳过（不重对齐）`
  - `跟随心跳: 帧差11.6 尺度源滑条 s=0.430 → 小窗(±12px) 分0.187 ov0.97 采纳`
  - `跟随心跳: 帧差11.6 尺度源滑条 s=0.230 → 全平移 分0.286 ov0.94（旧分0.712）采纳`
  - `跟随心跳: 帧差2.7 尺度源无（…）→ 判丢`
  - `跟随心跳: 投影显示但不开跟踪（confirmed=None track_active=True）` ← §4 第 3 条（接线）
  - `跟随心跳: 开态刚确立 → 本 tick 只做准备（不跟踪）` / `跟踪器未登记（没有可信投影）→ 不跟踪`
- bug① 修法取的是计划里的**第二种**：「只在"地图确认开着"时清 `_last_rgba`」——
  `_track_lost(why, is_open=True)`，`_last_rgba` 只在地图**确实开着**时清（假丢留图、真丢清图）。
  加上 `_track_tick` 开头的 `is_open` 早退，假丢根本不会发生。
- 新增断言（`experiments/smoke_app_offscreen.py`，8/8 → **10/10**）：地图判关时判丢 ⇒ 隐藏但留 `_last_rgba`；
  `_track_tick(roi, False)` ⇒ 直接返回（不跟踪、不判丢、不动作）。

**验收**：`python experiments/e33_follow_track.py` 10/10 ✓、`python experiments/e45_zoom_follow_replay.py` 3/3 ✓、
`python experiments/smoke_app_offscreen.py` **10/10** ✓；实机：重启 `python app.py` → 进图按热键 → **拖缩放**，
把心跳日志发回。

**T1 第二版（09-18 晚，读第二份实机日志之后；见 §2.4）—— 只动日志与判丢，不动任何阈值**：
1. **心跳防采样漏**（`app._track_beat`）：`帧差` 那条改记**本轮峰值**（上一次心跳以来的最大
   `mad` + tick 计数），并给 `force=True` 参数让**过闸 / 开态刚确立 / 面板不在 ROI** 这几条
   **绕过限流**（它们稀有且关键，被采样掉就等于没记）。原始文案变成
   `跟随心跳: 帧差0.00<闸1.2 画面没动 → 跳过（不重对齐）｜本轮峰值4.30 / 6 tick`。
2. **bug① 第二半**：`vision.map_open_from_roi` 返回第三项 `nav_ok`（主判据自己说开）；
   `_follow_tick → _maybe_track → _track_tick → _track_lost(why, is_open, trusted)` 一路带下去，
   清 `_last_rgba` 要求 `is_open and trusted`。
3. 断言：`smoke_app_offscreen.py` **10/10 → 12/12**（新增「只有雾兜底说开时判丢 ⇒ 留住
   `_last_rgba`」与「主判据说开时判丢 ⇒ 清掉」两条，把真丢/假丢的分界钉死）。
4. 回归：`e33` 10/10 ✓、`e45` 3/3 ✓、`e32` 7/7 ✓、`e30` **逐位不变**（261 张：旧开新关 17 /
   旧关新开 0 —— 注意 CLAUDE.md 里记的"176 张 / 14 张"是语料扩容前的旧数）、
   `eval_match` 21/24 + 拒答 20/44 + 控制组 4/11 ✓、`e26` 出图且对 21/44 (48%) + 出图但错 3 ✓、
   `verify_follow` 6/6 ✓、`verify_entrance_e2e` 17.13✓/17.11✓。

**T1 第三版（09-19，读第三份实机日志之后）—— ✅ 这才是真收口**：见 §2.5。补三个漏掉的
import（`TRACK_DEADBAND_S` / `TRACK_OVERLAP_MIN` / `FOLLOW_MAX_CAPTURE_ERRORS`）；
`_maybe_track` 给 `_track_tick` 套 try/except 把异常写进应用日志（**让失败可见**）；
顺手修 `mad is None`（本次跟随第一 tick）会让 `f"{mad:.2f}"` 抛 TypeError 的潜伏崩；
新增**真代码**回归 `experiments/e48_track_tick_live.py`（用用户当场截图驱动真
`_maybe_track`，断言零异常 + 真的跑到 `小窗`/`全平移` 判据）。同时修完 bug③。

### T1.5 · 「按此种子对齐」= 热键的对齐方式 —— ✅ done（同一天，见 §3 bug③）
用户原话：「点『按此种子对齐』按钮对齐也是不准的，也许我们可以把匹配时的对齐方式用在其他
跟随和对齐的时候。」→ `_realign` 改为**当场截屏 + 复用 `_two_stage_align`**（`hint_s` 取滑条
读数 ±0.01，`hint_icon` 取入口下拉对应引索图标），入口选错则回退全搜（= 旧行为）。
`e48` 第 3 节验收：旧行为偏 0/31/160px，新的三帧全 0px。


### T2 · UI 改造 —— ✅ **T2a + T2b 已落地（2026-09-19）**，T3 待做

> **落地结果**：主窗 **152px**（sizeHint 实测，原 ≈520/700）。`ceb20b0` = T2a（样本预览
> 自动消失），T2b = 主窗重排（下面 T2.0~T2.5 是当时的方案，落地细节见各节的「落地」行）。
> 回归：`smoke_app_offscreen` **37/37**（原 12/12）、`eval_match` 21/24 + 拒答 20/44 +
> 控制组 4/11、`verify_follow` 6/6、`e2e` 17.13✓/17.11✓、`e33` 10/10、`e45` 3/3、`e48` 零异常
> —— **全部与基线逐位一致**。
> ⚠️ **待用户实机点一遍**（离屏冒烟验证不了"好不好用"）：全新窗口看一眼五个按钮的位置、
> 点「▶ 按此种子重新对齐」是否用对话框里刚改的门、点状态行是否弹出日志窗。

#### T2.0 为什么改（现状事实，非猜测）

| # | 事实 | 位置 |
|---|---|---|
| 1 | 主窗 **固定宽 300 × 约 520px**（日志收起）／**约 700px**（日志展开），无边框 + `WindowStaysOnTopHint` ⇒ 常驻置顶遮游戏画面 | `app.py:247-251, 296-319` |
| 2 | 主窗常驻 **4 个下拉**（方向/门/楼层/入口）+ **3 个纠错按钮**（按此种子对齐 / 手框样本 / 手动选点）—— 这四个下拉**自动流程一个都不用**，只有纠错时才读 | `app.py:266-269, 278-280, 300-308` |
| 3 | 跟随·第二步收口后，**正常玩一局的交互只剩「按热键」一个动作**；上面那 7 个控件是纯占位 | 本文件 §2.5 |
| 4 | **样本预览窗 282×282 每次匹配都弹、且不自动消失**（只有 ✕ 或下次匹配才换）—— 用户点名要修 | `ui/preview.py:39-56`、`app.py:778-785` |
| 5 | 主窗文案还在指引用户去点**已经被折叠的按钮**（「手框样本/3点标定」「▶ 按此种子对齐」）⇒ 折叠后这些话会指向不存在的控件 | `app.py:429, 481, 487, 506, 797-798` |
| 6 | **LED 行**（`已启动 · Ctrl+Shift+F 已注册`，`word-wrap` 可到 2 行）与**日志区 150px** 都常驻 —— 用户 09-19 明确说这两个也不留主窗，**状态只留图上那样的一小行** | `app.py:254-257, 259-264, 316-317` |

**目标**：主窗高度 **≈190px**（现约 520/700 —— 09-19 用户看了第一版方案的「≤210px」后说不够狠，
**LED 行与日志区也撤**；随后又定「参考图」行**留**、「隐藏地图」**留**、「换种子」**上主窗**）；
正常流程一眼只看到「热键 + 一小行状态」；样本预览 **5 秒自动消失**。
**宽度 300 不动**（拖拽热区与滑块排版按它调的）。

#### T2.1 主窗重排（自上而下）

```
┌────────────────────────────────┐
│         🔴 一键匹配             │  44px  主按钮（=热键）
│ [▶ 按此种子重新对齐] [🔄 换种子] │  32px  用户 09-19：这两个都要在主窗
│ [👁 隐藏地图]      [⋯ 更多 ▾]   │  32px  用户 09-19：「隐藏地图我很常用」⇒ 留主窗
│ 参考图: 种子12 北-沙发门 一楼 ▾ │  22px  点开→「参考图…」对话框
│ 自动识别不确信(0.38) 方向未知…  │  22px  ← **就这一行**（现 `self.status` 的观感），悬停看全文
│ ●────────────────  地图透明度   │  26px  现成滑块，不动
└────────────────────────────────┘
```
≈ 44 + 32 + 32 + 22 + 22 + 26 + 边距 ≈ **190px**（原 ≈520/700）。

**用户 09-19 的按钮清单（5 个全在主窗，无待定项）**：
`🔴 一键匹配` · `▶ 按此种子重新对齐` · `🔄 换种子` · `👁 隐藏地图` · `⋯ 更多`。
⚠️ 按钮字面是**「按此种子**重新对齐**」**（用户纠正过：不是"按此种子匹配"）——
就是 09-19 修好的 `_realign` 改名上主窗（T1.5，`e48` 第 3 节三帧全 0px）。
原「手动纠错」子菜单因此**只剩两条**：手框样本匹配 / 手动选点重合。

**用户 09-19 追加的约束（第一版方案里 LED 行与日志区都还要常驻 —— 不够，全撤）**：

- **LED 行不常驻**：`led` 的文案（`已启动 · Ctrl+Shift+F 已注册` / `热键失败`）不再占一整行。
  推荐做法：状态行最左边一个 **8px 圆点**（`led` 改小 + 圆角样式、不显示文字），点它 = 原
  「ℹ 热键状态」，悬停显示 `已启动 · Ctrl+Shift+F 已注册` —— **零高度成本**，红/黄/绿告警不丢。
  ⚠️ 若连圆点也不要，就删掉主窗入口、状态信息只进「⋯ 更多 → 诊断 → 热键状态」；
  **`set_led` 的调用点一个都不删**（只改渲染方式），否则启动/热键失败的反馈会整个消失。
- **日志区不常驻**：`log_view`（现 150px）**从主窗布局移出**，放进懒创建的 `_log_window`
  （`QDialog`，非模态、可缩放）。入口：`⋯ 更多 → 诊断 → 运行日志`。
  ⚠️ **`log_view` 对象仍归 `MainWindow`**（同 combo 的套路），`_log_step` 的写入目标一个字不改；
  对话框**关闭不销毁** `log_view`，否则关窗期间那段日志全丢（那是排查实机问题时唯一的东西）。
  `settings.show_log` 语义改为「日志窗**启动时**是否打开」，默认改 **False**。
- **状态行就一行**：沿用现有 `_log_step` 里的 `status.setText(msg[:53] + "…")` 截断，**另加
  `status.setToolTip(全文)`** —— 截断看不全时悬停即可（用户那张图正是被截断的样子）。
  **不再需要 2 行**、不再 `word-wrap`。
  ⚠️ **这一行的代价要认**：日志窗默认关着之后，**状态行就是唯一的失败反馈**，而它是截断的。
  两条补偿（各一行代码，建议一起做）：
  ① `WARN`/`ERROR` 时状态行**染色**（黄/红），一眼能看出"这句是坏消息"；
  ② **点击状态行 = 打开日志窗**（复用 `_log_window`），想看全文不必翻菜单。
  不做这两条的话，"出问题了但看不出问题在哪"会比现在更严重。

其余（参考图下拉进对话框、剩余纠错按钮进菜单）：

- **下拉四件套进对话框**：新增 `_show_ref_picker()`（`QDialog`，内含 4 个 combo + `seed_label`）。
  **用户 2026-09-19 已选此方案**（备选「留主窗可折叠抽屉」被否）。
  ⚠️ **combo 对象仍归 `MainWindow` 所有，只是换父布局容器** —— `currentIndexChanged` 上挂的
  `_on_direction_changed` / `_resolve_seed` 连接**一个字都不能断**（`_realign` 读的就是
  `self.direction_combo.currentText()` 等）。这是本次**最容易回归**的点，验收必查。
- **纠错按钮进菜单**（`▶ 按此种子重新对齐` 除外 —— 它按用户要求回主窗了）：
  `⋯ 更多 → 手动纠错 →` 手框样本匹配 / 手动选点重合。
- **`⋯ 选项` 改名 `⋯ 更多`**，分为三组（`QMenu.addMenu` 子菜单）：
  - `手动纠错`：手框样本匹配 / 手动选点重合 / 「参考图…」（点开 `_show_ref_picker`）
  - `诊断`：运行日志 / 标记上次错→回收 / 素材管理 / 设置 / 热键状态
  - `退出`
- **`app._show_options_menu` 已有雏形可直接扩**（注释写着"UI改造B2：低频折叠"；⚠️ 计划里原记的
  `app.py:1049` 是旧行号，**现为 `app.py:1196`**）。
- **主窗文案跟着改**（T2.0 第 5 条）：`app.py:429/481/487/506` 这些指引句前缀加
  `⋯ 更多 → 手动纠错 →`；`app.py:797-798` 那句「▶ 按此种子对齐」**改成「▶ 按此种子重新对齐」**
  （它还在主窗，只是改了字）。**不做这一步，折叠就等于把纠错路径藏没了。**
- **主窗「参考图: 种子12 北-沙发门 一楼 ▾」这行留着**（用户 2026-09-19 定）。原 `seed_label`
  并入它；点开 = `_show_ref_picker()`（4 个下拉）。理由：它**是整个界面上唯一显示"当前选的是
  哪个种子"的地方**，撤掉后手选参考图在主窗没有任何回显。⇒ 主窗定稿 **≈160px**。

#### T2.2 样本预览 5 秒自动消失（用户点名）

`ui/preview.py` 内加 `QTimer(self)`（`setSingleShot(True)`），`set_sample()` 末尾 `start()`：

- **默认 5.0 秒**，到点 `self.hide()`（不 `close()`、不 `destroy`）。
- **悬停/拖动时暂停**：`enterEvent` 存 `remainingTime()` 并 `stop()`，`leaveEvent` 用剩余值重启；
  拖动（`mousePressEvent`/`mouseReleaseEvent`）同规。**用户正在看的时候不该消失**。
- **✕ 按钮保留**（立即消失）；`_show_sample_preview` 每次匹配 `set_sample` + `show()` 都会**重新计时**。
- **不留回看入口**（用户 2026-09-19 定）：消失就消失，想再看就再按一次热键。
  主窗**不加**「看样本」按钮，预览窗**不加** 📌 钉住按钮 —— 两个备选都被否。
- **时长可配**：`ui/settings.py` 的 `Settings` 加 `sample_preview_sec: float = 5.0`
  （`load()` 的白名单同步加该键），设置面板加 `QDoubleSpinBox`（0 ~ 30，步长 1，**0 = 不自动消失 = 旧行为**）。
- ⚠️ **硬约束：只 `hide()`/`show()`，绝不新建窗口、绝不 `setWindowFlags`。** 预览窗已设
  `WDA_EXCLUDEFROMCAPTURE`（`ui/preview.py:19-23`），重建 HWND 会丢截屏排除 affinity ⇒
  窗里的样本图被截进面板，**污染探明模板/雾占比/导航列 NCC/地图开合判据**，不只是匹配变差。
  验收里加一条断言：`hide()` → `show()` 前后 `winId()` 不变。
- **零算法风险**：只动 `SamplePreview` 内部的计时器，`_after_match`/手框路径的调用点一处不改。

#### T2.3 与 T1 的边界（别把两件事搅在一起）

- T1 的**实机复验尚未完成**（§8 第 1 条）⇒ T2 只允许碰：
  `app.py` 的 UI 构造段（`__init__` 布局 / `_show_options_menu` / 新增 `_show_ref_picker`）、
  `ui/preview.py`、`ui/settings.py`、以及 T2.0 第 5 条那几行文案。
- **不碰**：`core/` 全部、`ui/track.py`、`ui/follow.py`、`_follow_tick` / `_maybe_track` /
  `_track_tick` / `_realign` / `_two_stage_align` 的任何逻辑。
- 顺手记（**本轮不动，只留个记号**）：`_manual_sample_pick`（`app.py:681`）开头
  `if self._shot is None and not self._capture()` 与 bug③ 的 `_realign` **同形** ——
  手框用的也是「上一次热键那张图」，地图挪过之后框出来的坐标就对不上当前屏幕。未验证，
  留待 T2 之后单独确认。

#### T2.4 验收

1. **尺寸（改造前先量）**：在 `experiments/smoke_app_offscreen.py` 里打印 `win.sizeHint()`，
   改造前后各跑一次记下来（目标 **≈190px**）。**这是本次唯一的主 KPI**。
2. **离屏冒烟 + 新断言**（`experiments/smoke_app_offscreen.py`，现 12/12 → 更多）：
   - 主窗 5 个按钮都在且 `clicked` **已接线**：`btn_onematch` / `btn_realign`（文案 =
     按此种子重新对齐）/ `btn_swap`（换种子，T3 前先置灰）/ `btn_hide` / `btn_more`；
     再加 `_show_ref_picker` 与日志窗的入口；
   - `_show_ref_picker` 构造出的对话框里，**combo 就是 `win.direction_combo` 等本体**
     （`is` 判等），且改值后 `win._resolve_seed()` 能算出种子；
   - **日志窗**：打开 → 关掉 → 再打开，`log_view.toPlainText()` **不丢**（对象没被销毁）；
   - **LED 不常驻**：主窗布局里没有显示文字的 led（或已缩成圆点），但 `set_led(False, "…")`
     仍不抛且颜色状态可查；
   - `set_sample` 后 `_timer.isActive()`；`sample_preview_sec=0` ⇒ **不**启动；
   - 手动触发到点 ⇒ `preview.isVisible() is False`；
   - `hide()`→`show()` 前后 `preview.winId()` **不变**（钉死 affinity 约束）。
3. **手点一遍**（离屏冒烟验证不了 UI 好不好用）：
   - 新主窗高度、状态行是否够看（**被截断时悬停出全文**）；
   - 全屏进游戏，主窗**不挡**地图面板（面板 `FIXED_PANEL=(668,166,1064,569)`，主窗在左边缘
     `(8,60)` 起）—— 这是本次改造的**唯一目的**，必看；
   - **主窗** `▶ 按此种子重新对齐` 用**对话框里刚改的**门，结果与改造前逐位一致
     （这条同时验证 combo 搬进对话框后信号没断）；
   - 按热键 → 样本预览弹出、**约 5 秒后自己消失**、鼠标悬停时**不**消失；
   - `设置 → 样本预览时长 = 0` ⇒ 恢复"不自动消失"。
4. **回归照旧**（T2 理论上不该影响任何算法数字，但口径要求跑）：
   `eval_match.py` 21/24 + 拒答 20/44 + 控制组 4/11；`e26` 出图且对 21/44；
   `verify_follow` 6/6；`verify_entrance_e2e` 17.13✓/17.11✓；`e33` 10/10；`e45` 3/3；`e48` 零异常。

#### T2.5 不做（防范围蔓延）

- ❌ UI 美化 / 皮肤 / 打包（`CLAUDE.md` 锁）。
- ❌ **流程进度条**（`UI与流程改造计划.md` §五 那条建议）：它要常驻高度，与 T2「省屏幕」的目标
  直接冲突。若要进度反馈，改成**状态行里的一句话**（`状态: 截屏→匹配→对齐→投影` 逐步覆盖），
  不新增控件 —— **需用户点头再做**。
- ❌ 跟随·第三步（换种子）→ 那是 T3。
- ❌ 主窗宽度、`_refuse`（不投影）行为、两闸阈值。

#### T2.6 落地时与上述方案不同之处（2026-09-19，都是实现逼出来的）

1. **状态行按「像素」截断，不是按字数**。方案原写「沿用 `status.setText(msg[:53] + "…")`」——
   但那是两行布局下的写法，改成一行后 **56 个汉字 ≈616px 塞不进 284px**，`[:53]` 会硬切在
   字中间（用户截图里「建议手动确」就是这么来的）。改用
   `QFontMetrics.elidedText(msg, ElideRight, STATUS_MAX_PX=262)`。
2. **新增 `_set_status(msg, level)`，所有状态行写入都走它**。原来 `app.py` 里有 **15 处**
   直接 `self.status.setText(...)`（`_after_match` / `_realign` / `_mark_wrong` …）——
   那样会绕过截断、tooltip 和配色，**tooltip 还留着上一条的内容**（hover 出来是错的）。
   现已全部改到 `_set_status`。
3. **`show_log` 默认改 `False`，且 `settings.json` 里那条也改成了 `false`**
   （该文件在版本库里）。语义从「日志区显隐」变成「启动时是否打开日志窗」，
   `_apply_settings` 里那行 `log_view.setVisible(...)` 必须换成开/关日志窗 ——
   再直接隐藏 `log_view` 只会让日志窗里空一块、窗口还杵着。
4. **`FIX_PATH = "⋯ 更多→手动纠错→"` 常量**：折叠后指引文案统一用它拼，别再写裸按钮名
   （`app.py` 里 6 处已改）。
5. **新增 `_ClickLabel(QLabel)`**（`clicked` 信号）：状态行和「参考图」摘要行都要可点，
   为这两处装 eventFilter 不划算。
6. **顺手接上 `floor_combo.currentIndexChanged → _resolve_seed`**：摘要行里带楼层，
   不接的话改了楼层这行就陈旧了（`_seed` 本身只由方向+门决定，重算安全）。
7. **`btn_quit` 控件删了**：退出已进菜单，直接连 `self._quit`，不需要一个不在布局里的按钮。

### T3 · 换种子 + 会话内黑名单 —— ✅ done（2026-09-19）

**入口 = 主窗 `🔄 换种子` 按钮**（用户 2026-09-19 定，不是菜单项 —— 见 T2.1）。数据现成，
不需要新算法：`find_seed_by_entrance` 返回的 `res` 就是**入口层排名**，
日志实例：`入口匹配: 种子23(北-1沙发门) 墙重合48% top3=[(23,48%),(1,47%),(20,44%)]`。

**落地**（`app._swap_seed` / `_sync_swap_btn` / `_tried_seed`）：
- `res` 存进 `self._entrance_res`（+ 产出它的入口类型 `self._entrance_et`），在 `_after_match`
  **所有早退分支之前**存 —— 退化/哨兵分那两类虽然没投影，但用户正需要换种子。
- **点一下 = 把当前种子拉黑 + 试下一个候选**。点它的理由本身就是"这个种子不对"，
  若改成"等对齐失败才拉黑"，用户得点两次才见效。
- 候选 = 排名里第一个不在黑名单的；`_align_to` 重跑（当场截屏 + 两段式，与「重新对齐」**同一条路**）。
- 候选耗尽 ⇒ 状态行提示 + 按钮置灰；**按热键（`_entrance_pipeline_impl` 第一行）清空黑名单**
  （用户原话：「按『自动匹配』时就把黑名单重置防止匹配不到种子」）。按钮初始置灰（还没匹配过）。
- 换完把 `_select_ref(key, floor)` 走一遍 ⇒ 主窗「参考图」行跟着换，不会还显示被换掉的种子。
- 候选只有 3 个 ⇒ 最多换 2 次。想多换把 `app.py` 的 `top_n=3` 提到 5：**已核对安全** ——
  `_degenerate`（`app.py:98`）与 `_lead_frac`（`app.py:103`）**都只看 `res[0]`/`res[1]`**，
  多返回几个不改它们的值，**不影响任何闸**。代价只是入口层多算两个种子（改前跑 `e17_perf.py`）。

**⚠️ 写 T3 时真撞上的一个 bug（已修 + 已钉回归）**：黑名单一开始是拉黑 `self._seed` ——
那是**从下拉反解**出来的号，若 `key → 种子` 的反解与入口层排名里的种子对不上
（合成候选测试里就撞上：key「北-1门」反解成种子10，而候选里是种子1），就会拉黑一个
**不在候选表里**的号 ⇒ 候选表永不缩小 ⇒ **每次点击都重复试同一个候选、永远换不完**。
改成按 `self._tried_seed`（上一次**实际试过**的种子）拉黑。
`smoke_app_offscreen` 里有一条专门造这个反解不一致的回归（"★"那条）。

**验收**：`smoke_app_offscreen` 新增 9 条 T3 断言（候选取自**真引索**保证 key 能反解、
点一次/连点两次/耗尽/热键清空/★反解不一致仍换得完）**全绿**。
三处原始验收（`种子23 → top3 [(23),(1),(20)]` 那一幕 ⇒ 按一次给 1 或 20）由合成候选覆盖。
回归：`e48` 三帧逐值不变（0.038/1.00、0.055/1.00、0.263/0.86）、`e33` 10/10、`e45` 3/3、
`eval_match` 21/24 + 拒答 20/44 + 控制组 4/11、`e26` 出图且对 21/44(48%) + 错 3、
`verify_follow` 6/6、`e2e` 17.13✓/17.11✓ —— **与基线逐位一致**。

## 6. 硬约束提醒（违反 = 返工，详见 `CLAUDE.md`）

- 匹配口径每层只有一种：入口层 = 墙重合（闸 0.45）；对齐层 = 错配率（闸 0.30）。**禁 NCC 匹配、禁 sqdiff。**
- `cv2.minMaxLoc` 返回序是 `(minVal, maxVal, minLoc, maxLoc)`，**minLoc 是第 3 个**。
- 尺度步长 0.05，禁粗档；`ALIGN_SCALES` 下界 **0.30 别放宽**（放宽 = 分更漂亮、答案更离谱）。
- 只用真实截图，**禁合成面板**；一次性脚本只能进 `experiments/`；真值只写 `eval/labels.csv`。
- 每改 `core/` 跑 `eval_match.py` + 报告数字；改崩了 `git checkout` 重来。
- **`captures/`（97MB）不能删** —— `eval/labels.csv` 的 `hotkey_*.png` 在那里解析。
- ⚠️ 窗口化布局下导航列 NCC 只有 0.34（< 闸 0.45）⇒ **滑条读数恒失效**，且窗口化 capture 的面板几何不可信。
  本次实机日志是**全屏**（底行≈36）✓。别拿窗口化截图当对齐真值。

## 7. 运行 / 回归命令

```
cd "D:\claudecode out"
python app.py                                    # 主程序
python experiments/e33_follow_track.py           # 跟随·第二步回归（10/10）
python experiments/e45_zoom_follow_replay.py     # 缩放跟随回归（3/3）
python experiments/e47_zoom_motion_gate.py       # 帧差闸测量（本次新增，§2.2）
python experiments/e48_track_tick_live.py        # ★跟踪主体真代码回归（非复刻；改跟随必跑）
python experiments/smoke_app_offscreen.py        # 离屏冒烟（12/12）
python eval/eval_match.py                        # 旧口径（分母只算应答）
python experiments/e26_e2e_truth.py              # 真实口径（出图·对/错/藏图/拒答）
python eval/verify_follow.py && python eval/verify_entrance_e2e.py
```

## 8. 下次会话第一件事

**T1 已真收口（09-19，见 §2.5）**：病根不是帧差闸，是 `TRACK_DEADBAND_S` 漏 import 引发的
`NameError` —— **跟随·第二步自 `1b888e7` 起一次都没运行过**。第三份日志的 `本轮峰值`
（2.51~9.35）证明帧差闸清白，**阈值一个没动**。

1. **请用户实机复验**：重启 `python app.py` → 全屏进图 → 按热键 → **拖缩放再平移**。
   这次日志里该出现 `跟随心跳: 帧差… → 小窗(±12px) 分… 采纳` 与 `跟随: 投影跟着地图更新（…）`；
   若出现 `跟踪内部异常 #N（…）` 请把原文发回 —— 那是新加的异常兜底在报错，比静默好得多。
2. 顺带看「按此种子对齐」（§3 bug③）现在准不准：它已改成与热键**同一条路径**。
3. 复验通过后按 **T2（UI 改造，方案见 §5 T2.0~T2.5，2026-09-19 已出，待用户确认）→
   T3（换种子+黑名单）**。

**教训（写给下一个会话）**：三轮排查（`7818bc4` 尺度源协议 → `cd6b8f2` 心跳峰值）都在围着
帧差闸与「静默路径」打转，而真相是「**代码根本没跑到**」。以后遇到「实机没反应」，先花五分钟
写一个**驱动真代码**的离线脚本（像 `e48` 那样），**别先怀疑算法和阈值** —— 复刻版本的回归
通过，不等于真代码跑过。

